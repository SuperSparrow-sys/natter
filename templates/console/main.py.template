# Startet das Programm. Hier musst du nichts ändern - dein Code
# gehört nach u_main.py.
#
# Der Import führt u_main.py aus; `del` hält danach nur noch sauber
# auf, damit keine Prüfung den Namen für überflüssig hält.

import u_main

del u_main
