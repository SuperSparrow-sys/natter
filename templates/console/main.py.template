# Startet das Programm. Hier ist nichts zu ändern - der eigene Code
# gehört nach u_main.py.
#
# Der Import führt u_main.py aus; `del` räumt danach nur noch auf,
# damit keine Prüfung den Namen für überflüssig hält.

import u_main

del u_main
