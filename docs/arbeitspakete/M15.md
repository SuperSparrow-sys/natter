# Arbeitspaket M15 – was nach der Bestandsaufnahme übrig blieb

Siehe README.md, Abschnitt 5.2 (Komponentenpalette), 10 (Datenbank) und
13 (Diagramm-Editor). Kleinteilig wie M1–M14.

## Wie dieses Paket entstanden ist

Am 19. September 2026 standen in `docs/PLAN.md` und in den
Arbeitspaketen 112 unabgehakte Punkte. Auf die Frage, womit es
weitergehen soll, kam vom Nutzer die Antwort: „prüfe als erstes was
noch offen ist. viel wurde schon behoben und comittet."

Das hat sich bestätigt. Jeder der 112 Punkte wurde einzeln gegen den
Quelltext geprüft; **86 davon waren längst umgesetzt und nur nie
abgehakt worden** – darunter der komplette Eigenschaften-Dialog für
UML-Klassen (M9 Schritt 12, fünf Reiter, 22 Tests), beide Wege der
Quelltexterzeugung (Schritte 13 und 14, zusammen 78 Tests), alle vier
zusätzlichen Diagrammtypen, Zoom für alle drei Zeichenflächen,
Mehrfachauswahl, Knickpunkte, das Ziehen von Struktogrammblöcken und
ganz M10. Vier weitere waren gar keine Aufgaben, sondern getroffene
Entscheidungen (kein `StopIteration`-Eintrag, keine Screenshot-Sammlung
im Repository, zwei Daueraufgaben ohne Ende); sie stehen jetzt als
solche da statt als ewig offene Kästchen.

**Wirklich offen waren 22** – die 22 stehen unten, aufgeteilt auf sechs
Schritte und so kleinteilig aufgeschlüsselt wie in M1 bis M14.

Übrig bleiben die Punkte in diesem Dokument. Sie sind nach Nutzen für
den Unterricht geordnet, nicht nach Aufwand.

## Der Maßstab

Alle Punkte hier werden an derselben Frage gemessen wie der Rest des
Projekts: **kann eine Schülerin damit etwas bauen, das sie vorher nicht
bauen konnte?** Ein Menü in einem Fenster, ein gezeichnetes Bild, eine
Datenbankverbindung, die man anklickt statt abtippt – das sind die
Lücken, die beim Benutzen auffallen.

## Reihenfolge der Umsetzung

Am 19. September 2026 auf Nutzer-Wunsch festgelegt („mache jetzt eine
Liste was wann zu tun ist und gehe diese nacheinander durch"): M15 wird
in einem Zug zu Ende gebracht, Block für Block, jeder Block mit Tests,
Commit und Push.

Die Reihenfolge ist keine Fleißreihenfolge, sondern eine
Abhängigkeitsreihenfolge:

| # | Block | Warum an dieser Stelle |
|---|---|---|
| 0 | Menüs einchecken, `ruff format` in AGENTS.md klären — **erledigt** | Fertige Arbeit gehört committet, bevor neue darauf aufsetzt |
| 1 | Datenbank vereinfachen (Abschnitt 3, neu geschnitten) | Vorgezogen, weil dieser Block Code **wegnimmt** und die Beispielprojekte anfasst. Alles Spätere setzt danach auf einem aufgeräumten Stand auf |
| 2 | `PaintBox` und `Canvas` (Abschnitt 2) | Steht für sich allein, hängt an nichts und fehlt im Unterricht am unmittelbarsten |
| 3 | Die übrigen Komponenten (Abschnitt 4) | `MaskEdit`, `DateEdit`, `TimeEdit`, `Calendar`, `HtmlViewer`, `Sound`, dazu `on_click` für alle sichtbaren Komponenten. Sechs kleine, voneinander unabhängige Stücke |
| 4 | Lineale, Hilfslinien, Minimap (Abschnitt 5) | Diagramm-Editor, berührt `pcl` nicht – lässt sich unabhängig vom Rest bauen |
| 5 | Restliche Kleinigkeiten (Abschnitt 6) | `NatterDatenError` im Fehlerkatalog, `plt.show()`-Abnahmetest, Abstände und Ausrichtung |
| 6 | Abschluss | Volle Testsuite, `docs/PLAN.md` nachführen, M15 abhaken |

### Vorab getroffene Entscheidungen

Damit der Lauf ohne Rückfragen durchläuft, stehen die offenen
Schnittstellenfragen hier vorab beantwortet:

- **`Prop(date)` und `Prop(time)`** mit den Python-Standardtypen
  `datetime.date`/`datetime.time`, in der `.pfm` als ISO-Zeichenkette,
  in der Oberfläche deutsch dargestellt (`TT.MM.JJJJ`, `hh:mm`). So
  bleibt der Code, den eine Schülerin schreibt, gewöhnliches Python
- **`HtmlViewer` auf `QTextBrowser`**, nicht `QWebEngineView`. Für den
  Unterricht reicht einfaches HTML, und `QWebEngineView` wöge über
  100 MB in der gebauten Exe
- **Die Datenbank wird kleiner, nicht größer** – siehe den folgenden
  Abschnitt

### Die Kehrtwende bei der Datenbank

Ursprünglich standen hier drei Datenbank-Punkte: die fünf
Datenbankkomponenten als Symbole auf dem Formular (Abschnitt 3), ein
dritter Palettenreiter dafür, und die Zugangsdaten verschlüsselt im
Windows Credential Store (Abschnitt 6).

Der Credential Store war als Absicherung gedacht:
`MySQLConnection.password` ist ein `Prop`, und sobald eine Verbindung
als Symbol auf dem Formular läge, schriebe der `.pfm`-Schreiber das
Passwort im Klartext in eine Datei, die Lernende abgeben und
herumtragen. Auf diese Begründung hin hat der Nutzer am 19. September
2026 nicht die Absicherung bestellt, sondern die Ursache gestrichen:
**„nimm das passwort raus und mache die datenbank abfrage einfacher.
das wird zu kompliziert oder?"**

Das ist der richtige Zug. Wenn eine Funktion Infrastruktur braucht, um
sicher zu sein, ist die Funktion falsch zugeschnitten – und eine
Schicht, die eine Schülerin nicht sieht, aber verstehen müsste, wenn
etwas klemmt, ist in einer Unterrichts-IDE ein Verlust. Drei
Entscheidungen folgen daraus:

- **Nur noch SQLite.** `MySQLConnection` und die Abhängigkeit PyMySQL
  fallen weg. Eine Datenbankdatei neben dem Programm läuft ohne Server,
  ohne Netz und ohne Zugangsdaten auf jedem Schulrechner. Damit gibt es
  in ganz Natter kein Datenbank-Passwort mehr – weder in der `.pfm`
  noch in einem Schlüsselspeicher
- **Der kurze Weg wird der Normalweg.** `query()`/`execute()` mit
  Schlüsselwort-Parametern und Auto-Commit statt des Lazarus-Cursor-
  Tanzes aus `open()` / `while not eof` / `field_by_name()` / `next()` /
  `close()` samt eigener `SQLTransaction`
- **Abschnitt 3 entfällt ersatzlos.** Wenn eine Abfrage eine Zeile Code
  ist, lohnt sich ein Symbol auf dem Formular nicht mehr. Kein dritter
  Palettenreiter, keine Designzeit-Verbindung

---

## 1. `MainMenu` und `PopupMenu` — erledigt

Die größte Lücke. Ein Schülerprogramm mit Menüleiste – „Datei →
Beenden", „Hilfe → Über" – war in Natter **nicht baubar**.
`pcl/components/standard.py` hielt das im eigenen Modulkopf seit M1
selbst fest. In Lazarus gehören beide zum Reiter „Standard" und werden
über den Menü-Editor gefüllt; ein Menü ist dort ein Symbol auf dem
Formular.

Die Vorarbeit war da: `Control.nur_im_designer` gibt es seit dem
`Timer` (M14). Eine Komponente mit dieser Angabe zeigt im Designer ein
gezeichnetes Symbol und versteckt sich im laufenden Programm – genau
das Verhalten, das ein Menü braucht.

### 1.0 Vorarbeit: die Palette verdrahtet jetzt alle Reiter

- [x] Die Reiter der Komponentenpalette standen an drei Stellen: als
  zwei Konstanten im Modul, als zwei Zuweisungen im `__init__` und im
  Hauptfenster als vier `connect`-Aufrufe auf genau diese beiden
  Listen. Ein dritter Reiter hätte seine Kacheln gezeigt und sich
  nicht aufs Formular legen lassen. Jetzt stehen sie nur noch in
  `REITER`, das Hauptfenster verbindet `palette.listen`, und wer
  „alle Komponenten" meint, nimmt `ALLE_KOMPONENTEN` – der
  Eigenschaften-Rundlauf, die Tooltip- und die Symbolprüfung tun das
  und erfassen damit auch einen später ergänzten Reiter von selbst
- [x] Vier Tests, einer davon hält genau den Fehler fest: jede Liste
  der Palette muss im Hauptfenster Empfänger für `itemActivated` und
  `itemClicked` haben

### 1.1 Die Komponenten

- [x] `MainMenu` und `PopupMenu` als `Control` mit
  `nur_im_designer = True` – in `pcl/components/menus.py` statt in
  `system.py` wie geplant: mit Editor-Datenmodell, Prüfung und zwei
  Symbolen wäre `system.py` (bisher nur der `Timer`) unübersichtlich
  geworden
- [x] Die Symbole werden gezeichnet, nicht als Datei beigelegt – wie
  `_ZeitgeberSymbol`. **Zwei verschiedene:** das Hauptmenü als Fenster
  mit blauer Leiste und aufgeklapptem Menü, das Klappmenü ohne
  Fensterrahmen, dafür mit dem Mauszeiger davor. Zwei gleich
  aussehende Symbole wären schlimmer als gar keines
- [x] Die Einträge sind **strukturierte Datensätze**: `name`,
  `caption`, `shortcut`, `enabled`, `checked`, `separator`,
  `on_click`, `children`. Derselbe Grundsatz wie beim UML-Dialog –
  was Felder hat, wird nicht als Zeichenkette gespeichert
- [x] Verschachtelung bis zur zweiten Ebene; eine dritte wird beim
  Zuweisen mit einer deutschen Meldung abgelehnt
- [x] Ein Tippfehler im Feldnamen („childs" statt „children") fällt
  sofort auf, statt still einen ganzen Teilbaum verschwinden zu lassen
- [x] Zur Laufzeit baut `MainMenu` eine echte `QMenuBar` im Fenster
  auf, `PopupMenu` ein `QMenu` auf die rechte Maustaste (`popup_menu`
  an jeder sichtbaren Komponente, wie `TControl.PopupMenu`)
- [x] `on_click` je Eintrag: der Name einer Methode des Formulars. Ein
  Name, zu dem es keine Methode gibt, lässt den Eintrag wirkungslos,
  statt den Aufbau des Fensters mitzunehmen – sonst startete das
  Programm bei einem Tippfehler gar nicht mehr

### 1.2 Der Menü-Editor

- [x] **Menü-Editor als Dialog** (`ide/inspector/menue_editor.py`),
  nicht als Bearbeiten auf dem Formular – dieselbe Entscheidung wie
  beim UML-Eigenschaften-Dialog. Links der Baum, rechts die Felder,
  darüber Neu/Untereintrag/Trennlinie/Löschen/▲/▼, unten Schließen,
  Anwenden, OK
- [x] Geöffnet per Doppelklick auf das Symbol, per F2 und über die
  Zeile `entries` im Objektinspektor – alle drei öffnen denselben
  Dialog
- [x] Ein Dialogdurchgang ist **ein** Undo-Schritt; „Schließen"
  verwirft, was seit dem letzten „Anwenden" geändert wurde
- [x] **Beim Bauen selbst hineingelaufen:** PySide wandelt ein `dict`
  beim Ablegen in den Item-Daten eines `QTreeWidget` in eine
  `QVariantMap` und wieder zurück. Was herauskommt, ist gleich, aber
  nicht dasselbe Objekt – ein `gewaehlt["children"].append(...)`
  arbeitete an einer Kopie und verpuffte spurlos. Am Baumeintrag hängt
  jetzt sein **Pfad** („0.2") statt des Datensatzes

### 1.3 Speichern und Erzeugen

- [x] `entries` als neue Art von Eigenschaft (`BAUM_EIGENSCHAFTEN` in
  `pcl/properties.py`), parallel zu den Sammlungen `items`/`lines`.
  Objektinspektor, `.pfm`-Schreiber und Codeerzeugung kennen sie damit
  an genau einer Stelle
- [x] In der `.pfm` steht nur, was vom Standard abweicht – sonst
  stünde hinter jedem Eintrag dreimal die Vorgabe
- [x] Im erzeugten `u_*_design.py` ein Eintrag je Zeile, Untereinträge
  eingerückt darunter. Ein Menü mit drei Untermenüs ergab sonst eine
  Zeile von über 800 Zeichen; gelesen wird die Datei selten, aber wenn,
  dann weil etwas klemmt
- [x] Die Schlüssel stehen sortiert, damit zweimal Erzeugen aus
  derselben `.pfm` zweimal denselben Quelltext ergibt
- [x] Beide im Palettenreiter „Standard", am Ende wie in Lazarus, mit
  eigenen SVG-Symbolen im Stil der übrigen (Farben und Strichstärken
  aus der Symbolordnung, geprüft von `tests/test_assets_symbole.py`)
- [x] In `docs/komponenten.md` dokumentiert – die verbindliche
  Schnittstelle, die Lernende über „Hilfe → Komponenten-Referenz"
  auch wirklich zu sehen bekommen

### 1.4 Was der echte Probelauf gefunden hat

Beides hätten die Tests nicht gefunden, weil sie das Falsche geprüft
hätten:

- [x] **Im Menü stand „Ctrl+N", obwohl im Editor „Strg+N" eingetragen
  war.** Qt schreibt die Tastennamen selbst und bräuchte dafür
  Übersetzungsdateien, die PySide6 nicht mitliefert. In einer
  durchgehend deutschen Oberfläche geht das nicht. Der Weg darum herum
  ist Qts eigener: steht im Text einer Aktion ein Tabulator, zeigt Qt
  alles dahinter als Kürzelspalte und schreibt nichts Eigenes hin. Die
  Tests hatten die Tastenfolge geprüft, nicht ihre Beschriftung
- [x] **„(1 Einträge)"** stand im Objektinspektor, sobald ein Menü
  genau einen obersten Eintrag hatte. Betraf auch `items` und `lines`
- [x] Das erste gezeichnete Symbol war zu grob (`seite / 16` sah aus
  wie ein Balkendiagramm). Jetzt `seite / 26` – „filigraner, wie in
  Lazarus", wie der Nutzer es für die Symbole festgelegt hat

### 1.5 Geprüft

- [x] 65 neue Tests (`tests/test_components_menus.py`,
  `tests/test_menue_editor.py`): Datensätze, Prüfung, Tastenkürzel,
  Editor (anlegen, umsortieren, löschen, Untereintrag, Anwenden/
  Schließen), Designer-Weg mit Undo, `.pfm`-Rundlauf, erzeugter
  Quelltext – der auch wirklich ausgeführt und im fertigen Fenster
  nachgesehen wird
- [x] **Echter Probelauf:** ein Notizblock-Programm mit drei Menüs,
  Untereinträgen, Trennlinie, Tastenkürzeln und einem angekreuzten
  Eintrag gebaut, gestartet, das Menü aufgeklappt, einen Eintrag
  angeklickt – die Methode lief, der Text landete im Memo. Das Memo
  stand dabei um die Leistenhöhe tiefer, wie es soll
- [x] Im Designer angesehen: Symbol auf dem Formular, beide Kacheln in
  der Palette, `mm_haupt: MainMenu` im Komponentenbaum, Zeile
  `entries` im Objektinspektor mit ihrem Kurzhinweis

## 2. `PaintBox` und `Canvas` — erledigt

Zeichnen kommt im Unterricht vor. In Natter gibt es dafür bisher nur
`Shape`, also fertige Formen; freies Zeichnen mit Koordinaten fehlt.

**Eine Angabe von früher ist dabei zu korrigieren:** hier stand, das
Lazarus-Referenzprojekt `b_schneefigur` bestehe aus `TPaintBox` und
`TCanvas`. Das stimmt nicht – nachgesehen in
`tests/daten/lazarus/b_schneefigur/unit1.lfm`: die Schneefigur ist aus
elf `TShape` zusammengesetzt, und `Canvas` kommt in **keinem** der 19
Referenzprojekte vor. `PaintBox` ist also eine echte Lücke, aber keine,
die ein Referenzprojekt belegt. Sie wird trotzdem gebaut – freies
Zeichnen mit Koordinaten ist Unterrichtsstoff, und ohne `PaintBox`
lässt sich in Natter kein einziges Pixel setzen –, und die
Sichtprüfung am Ende läuft folgerichtig gegen ein eigens gebautes
Zeichenprogramm statt gegen `b_schneefigur`.

- [x] `PaintBox` als sichtbare Komponente mit weißer Fläche und
  Rahmen im Designer
- [x] `Canvas` mit den Lazarus-Namen in deutscher Nutzung:
  `move_to`, `line_to`, `rectangle`, `ellipse`, `text_out`,
  `pen` (Farbe, Breite), `brush` (Farbe, Füllart) – `Brush` gibt es in
  `pcl/components/additional.py` bereits für `Shape` und wird
  wiederverwendet statt nachgebaut
- [x] Gezeichnet wird in ein `QPixmap`, das die `PaintBox` in ihrem
  `paintEvent` zeigt. Sonst wäre alles Gezeichnete beim ersten
  Neuzeichnen des Fensters weg – ein Fehler, der Lernende ratlos macht,
  weil ihr Code richtig aussieht
- [x] `on_paint`-Ereignis, damit sich auch neu zeichnen lässt
- [x] `clear()` löscht die Fläche
- [x] Tests: jede Zeichenoperation verändert das Pixmap an der
  erwarteten Stelle (Pixelfarbe prüfen), Größenänderung behält den
  Inhalt, `clear()` leert, `.pfm`-Rundlauf
- [x] Sichtprüfung: ein kleines Zeichenprogramm gebaut und wirklich
  gestartet – Rechteck, Ellipsen, `fill_rect`, Linien, `text_out`,
  Stift- und Füllfarbe umgestellt, `clear()` geleert. Das Bild steht
  (Bildschirmfoto abgenommen, Pixelfarben im laufenden Programm
  nachgemessen)

### 2.1 Was beim Bauen anders entschieden wurde

- [x] **Keine Kantenglättung.** Qt glättet auf Wunsch, Lazarus'
  `TCanvas` nie. Hier hängt mehr daran als das Aussehen: mit Glättung
  liegt an der Kante einer roten Linie nicht Rot, sondern eine
  Mischfarbe – `canvas.pixels[x, y]` gäbe `#e1958d` zurück, wo eine
  Schülerin `#c42b1c` erwartet, und „ist der Punkt rot?" wäre nicht zu
  beantworten. Sechs Tests sind genau darüber zuerst umgefallen
- [x] **Eigene `Pen`- und `Brush`-Klassen** statt des `Brush` von
  `Shape`, den dieses Paket oben wiederverwenden wollte. `Shape.brush`
  kennt nur `color`, eine `Canvas` braucht zusätzlich `style` (gefüllt
  oder nur Umriss) – und ein `Shape` würde dieses `style` stillschweigend
  ignorieren. Eine Eigenschaft, die je nach Besitzer wirkt oder nicht,
  ist schlimmer als fünfzehn Zeilen doppelt
- [x] **Die Fläche folgt `width`/`height` sofort**, nicht erst über
  `resizeEvent`. Qt stellt das Ereignis für ein noch nicht gezeigtes
  Widget in die Schlange; `self.pb_bild.width = 300` im `form_create`
  hätte sonst erst irgendwann später vergrößert und bis dahin in der
  alten Größe gezeichnet. Darüber sind beim ersten Lauf zwanzig Tests
  gefallen
- [x] `pixels[x, y]` dazugenommen (wie `Canvas.Pixels[x, y]` in
  Lazarus): einen einzelnen Punkt setzen und seine Farbe lesen. Im
  Unterricht die einfachste denkbare Zeichenübung – und das, was die
  Tests prüfbar macht
- [x] **Erledigt mit Abschnitt 4: Zeichnen mit der Maus.**
  `TPaintBox` in Lazarus hat `OnMouseDown`/`OnMouseMove`/`OnMouseUp`;
  ohne die geht die klassische Übung „mit der Maus malen" nicht. Das
  gehört nicht in diesen Abschnitt, sondern zu dem Punkt in Abschnitt 4,
  der Maus-Ereignisse für **alle** sichtbaren Komponenten in `Control`
  vorsieht – einmal gebaut, wirkt es für `Label`, `Shape` und `PaintBox`
  zugleich

## 3. Die Datenbank wird einfacher — erledigt

Ersetzt den ursprünglichen Abschnitt 3 („Nicht sichtbare Komponenten im
Designer"). Warum, steht oben unter „Die Kehrtwende bei der Datenbank";
kurz: der Nutzer hat die Ursache gestrichen statt sie abzusichern.

### 3.1 MySQL/MariaDB fällt weg

- [x] `MySQLConnection` aus `pcl/components/data_access.py` entfernen,
  mitsamt `host_name`, `port`, `user_name` und `password`. Damit gibt
  es in ganz Natter kein Datenbank-Passwort mehr
- [x] PyMySQL aus den Abhängigkeiten in `pyproject.toml`. Eine
  Abhängigkeit weniger in der gebauten Exe
- [x] Die MySQL-Felder aus dem Datenbank-Panel
  (`ide/database/panel.py`) entfernen; was bleibt, ist die Auswahl
  einer `.sqlite`-Datei
- [x] README (Abschnitt 2, 10), `docs/komponenten.md` und
  `docs/entwicklung.md` nachziehen – überall dort steht heute
  „SQLite und MySQL/MariaDB"

### 3.2 Der kurze Weg wird der Normalweg

Heute schreibt eine Schülerin für eine einzige Abfrage neun Zeilen:
`sql` setzen, `params` setzen, `open()`, `while not eof`, je Spalte ein
`field_by_name(...).as_string`, `next()`, `close()` – dazu eine eigene
`SQLTransaction` mit `commit()` nach jeder schreibenden Anweisung. Das
ist `TSQLQuery` aus Lazarus, originalgetreu. In Python liest es sich
falsch.

- [x] `SQLite3Connection(pfad)` öffnet die Datenbank direkt im
  Konstruktor. `connected` bleibt als Eigenschaft bestehen, wird aber
  im Schülercode nicht mehr gebraucht
- [x] `query(sql, **parameter)` liefert die Zeilen als Liste von
  `dict`s: `zeile["inhaber"]`. Ein `dict` ist genau das, was Lernende
  zu diesem Zeitpunkt schon kennen – ein eigener Zeilentyp wäre eine
  neue Vokabel ohne Gegenwert
- [x] `query_one(sql, **parameter)` für die erste Zeile oder `None`
- [x] `execute(sql, **parameter)` für INSERT/UPDATE/DELETE, mit
  **Auto-Commit** und der Anzahl betroffener Zeilen als Rückgabe
- [x] Die Parameter bleiben `:name`-Platzhalter und werden als
  Schlüsselwortargumente übergeben. Der Schutz vor SQL-Injection – im
  Lehrgang eigens erklärt – bleibt damit genau derselbe
- [x] `commit()` und `rollback()` wandern an die Verbindung, für den
  Fall, dass jemand mehrere Anweisungen zusammenfassen will
- [x] `SQLTransaction` entfällt. Mit Auto-Commit hat sie keine Aufgabe
  mehr, und eine Komponente, die man nur anlegt, um `commit()` darauf
  zu rufen, erklärt sich nicht von selbst
- [x] `SQLQuery` und `DataSource` bleiben – die Data Controls bauen
  darauf auf –, stehen aber nicht mehr im Lehrgang und in der
  Komponenten-Referenz als der Weg da

### 3.3 Die Data Controls werden prüfbar

Der offene Punkt aus M11: der Eigenschaften-Rundlauf
(`tests/test_eigenschaften_rundlauf.py`) kann `DBGrid` und die übrigen
`DB*`-Komponenten nicht mitprüfen, weil ihr Konstruktor eine
`DataSource` verlangt, der Designer Komponenten aber mit `typ(formular)`
allein erzeugt.

- [x] `data_source` wird im Konstruktor optional (`None`). Ohne
  Zuordnung zeigt ein `DBGrid` eine leere Tabelle, statt beim Anlegen
  zu scheitern. `DBGrid(formular, quelle)` funktioniert unverändert
  weiter
- [x] `DBGrid.show_rows(zeilen)` zeigt eine Liste von `dict`s
  unmittelbar an – der kurze Weg aus 3.2 endet damit in einer Zeile:
  `self.g_konten.show_rows(self.db.query("SELECT ..."))`
- [x] Die `DB*`-Komponenten laufen danach im Eigenschaften-Rundlauf mit

### 3.4 Beispiele, Tests, Doku

- [x] `06_Kontoverwaltung` auf den kurzen Weg umbauen und wirklich
  starten – das Beispiel ist der Ort, an dem Lernende den Umgang
  ablesen
- [x] Die vorhandenen Datenbank-Tests auf die neue Schnittstelle
  ziehen; neue Tests für `query`/`query_one`/`execute`, Auto-Commit,
  Parameter, Fehlerfall bei unbekannter Spalte
- [x] `docs/komponenten.md` und README zeigen den kurzen Weg

### 3.5 Was beim Schreiben der Doku aufgefallen ist

- [x] **`data_source` nachträglich zuzuweisen wirkte nicht.** Beim
  Formulieren des Beispiels für die Komponenten-Referenz
  (`self.g_konten.data_source = self.ds_konten`) stellte sich heraus,
  dass genau diese Zeile nichts tut: `data_source` war ein einfaches
  Attribut, und die Anmeldung bei der `DataSource` geschah
  ausschließlich im Konstruktor. Die Zuweisung lief durch, die Tabelle
  blieb stumm leer – der stillste aller Fehler, und einer, der erst
  durch 3.3 entstand: solange die Datenquelle Pflicht war, konnte sie
  gar nicht nachträglich kommen. `data_source` (und `list_source` der
  `DBComboBox`) sind jetzt echte Eigenschaften, deren Setter anmeldet
  und neu zeichnet. Drei Tests halten es fest


## 4. Die übrigen fehlenden Komponenten — erledigt

Keine davon blockiert etwas, alle vier sind kleine, abgeschlossene
Stücke Arbeit.

- [x] `MaskEdit` – Textfeld mit Eingabemaske (Datum, Postleitzahl,
  Telefonnummer) über `QLineEdit.setInputMask`
- [x] `DateEdit` und `TimeEdit` über `QDateEdit`/`QTimeEdit`, mit
  deutschem Format (`TT.MM.JJJJ`, `hh:mm`)
- [x] `Calendar` über `QCalendarWidget`, deutsche Monats- und
  Tagesnamen
### 4.1 Was beim Bauen dazukam oder anders wurde

- [x] **`Prop` kennt jetzt `date` und `time`.** Das war die
  Schnittstellenentscheidung, die diese drei Komponenten so lange
  aufgehalten hat. Sie berührt vier Stellen, und alle vier stehen
  jetzt in `pcl/properties.py` an einer Quelle: `text_aus_wert` und
  `wert_aus_text` für den Objektinspektor (deutsch: `23.11.2026`),
  `pfm_wert` und `wert_aus_pfm` für die Datei (ISO: `2026-11-23`).
  Der Codegen schreibt `date(2026, 11, 23)` und setzt
  `from datetime import date, time` nur dann in die erzeugte Datei,
  wenn wirklich ein Datum darin vorkommt
- [x] **Ein eigener Palettenreiter „Eingabe"** für die vier. In
  Lazarus stehen sie verstreut in „Misc" und „Common Controls"; hier
  gehören sie sichtbar zusammen. Möglich erst, seit Schritt 1.0 die
  Palette über **alle** Reiter verdrahtet - vorher wäre der dritte
  Reiter stumm geblieben
- [x] **`Sound` ist keine `Control`.** Sie liegt nicht auf dem
  Formular, sondern wird im Code erzeugt, wie eine
  Datenbankverbindung. Ein Symbol im Designer brächte nichts ein, was
  eine Zeile Code nicht auch tut
- [x] **`MaskEdit.is_complete` wieder entfernt**, nachdem es gebaut
  war. Es reichte Qts `hasAcceptableInput()` durch, und das meldete
  nachgemessen auch bei halb gefülltem Feld „fertig". Eine
  Eigenschaft, die lügt, ist schlimmer als keine - die Länge des
  Textes sagt dasselbe und stimmt
- [x] **Ein Fehler, den erst ein Test fand:** `feld.text = "abcde"`
  bei der Maske `00000` ließ die Prop auf `"abcde"` stehen, während
  das Feld leer blieb - `text` behauptete etwas, das man nirgends
  sah. Über `textChanged` kam es nicht zurück, weil Qt bei einem
  ohnehin leeren Feld gar keine Änderung meldet; jetzt holt
  `_text_gleichziehen()` nach jedem Setzen, was wirklich drinsteht

- [x] **Vorher zu entscheiden:** `Prop` kennt bisher keinen Datumstyp.
  Das war der Grund, diese drei in M14 auszulassen – eine
  Schnittstellenentscheidung, die kein Referenzprojekt absichert. Der
  Vorschlag: `Prop(date, …)` und `Prop(time, …)` mit den
  Python-Standardtypen, in der `.pfm` als ISO-Zeichenkette gespeichert,
  in der Oberfläche deutsch dargestellt. Dann bleibt der Code, den eine
  Schülerin schreibt, gewöhnliches Python
- [x] `HtmlViewer` – zeigt HTML an, ohne den Browser zu öffnen.
  **Vorher entscheiden und festhalten:** `QTextBrowser` reicht für
  einfaches HTML und kostet nichts; `QWebEngineView` kann echtes Web,
  wiegt aber über 100 MB in der gebauten Exe. Für den Unterricht
  spricht alles für `QTextBrowser`
- [x] `Sound` – Klang abspielen in GUI-Programmen. `pcl.crt.beep()`
  gibt es für Konsolenprogramme, eine Komponente nicht. `QSoundEffect`
  für `.wav`, dazu `beep()` als einfachster Fall. (Hier stand
  `piepton()` – die Funktion heißt seit jeher `beep`; beim Bauen
  nachgesehen.)
- [x] Je Komponente: Symbol, Palettenplatz, `.pfm`-Rundlauf,
  Objektinspektor-Eigenschaften, Tests
- [x] **Maus-Ereignisse für alle sichtbaren Komponenten — erledigt.**
  `on_click`, `on_double_click`, `on_mouse_down`, `on_mouse_move`,
  `on_mouse_up` stehen jetzt in `Control` und gelten damit für jede
  sichtbare Komponente. Vorher war `on_click` nur am `Button`
  verdrahtet; `Label` und `Image` hatten je eine eigene
  QLabel-Unterklasse, die `mousePressEvent` abfing – zwei Nachbauten
  derselben Sache, und für `Shape`, `Panel` oder die frische `PaintBox`
  gab es gar nichts. Beide Nachbauten sind entfallen.
  - Ein **Ereignisfilter** statt einer Widget-Unterklasse je
    Komponente: die, die ihr Widget fertig von Qt bekommen
    (`QPushButton`, `QComboBox`), ließen sich anders gar nicht
    abfangen. Der Filter gibt immer `False` zurück, sonst käme man in
    ein `Edit` nicht mehr hinein
  - `Button` behält sein natives Klicksignal
    (`_klick_kommt_vom_widget`) – ein Knopf reagiert auch auf die
    Leertaste, und ohne die Angabe hätte sein `on_click` doppelt
    ausgelöst
  - `on_click` kommt beim **Loslassen** und nur, wenn vorher auf
    derselben Komponente gedrückt wurde. Wer danebenzieht, hat es sich
    anders überlegt
  - Die Koordinaten zählen ab der linken oberen Ecke der Komponente,
    wie in Lazarus. `EREIGNIS_PARAMETER` legt fest, welches Ereignis
    `x`/`y` mitbringt; der Ereignis-Generator legt die Methode danach
    mit `(self, sender, x, y)` an, und der Platzhalter im Designer
    verträgt beide Signaturen
  - Nicht sichtbare Komponenten (`Timer`, `MainMenu`, `PopupMenu`)
    bekommen keinen Filter – eine Maus kann sie nicht treffen
  - 61 Tests, dazu ein gestartetes Malprogramm: gedrückt, gezogen,
    losgelassen – die Linie steht

## 5. Lineale, Hilfslinien und Minimap im Diagramm-Editor — erledigt

Die drei letzten ausgegrauten Einträge im Menü „Ansicht" des
Diagramm-Fensters (M9, Teilschritt 2b). Zoom, Raster, Seitenränder und
Layout-Hinweise sind dort längst aktiv.

- [x] Lineale an oberem und linkem Rand, in Millimetern passend zum
  Seitenformat, mit einer Marke, die der Maus folgt
- [x] Hilfslinien: aus dem Lineal ziehbar, einrastend, mit der `.pdiag`
  gespeichert (neues Feld `guides`, Schema erweitern)
- [x] Minimap unten rechts: das ganze Diagramm verkleinert, der
  sichtbare Ausschnitt als Rahmen, Klick springt dorthin
- [x] Alle drei über das Menü „Ansicht" an- und abschaltbar, Zustand
  in den `QSettings` gemerkt
- [x] Tests: an/aus, Lineal rechnet Zoomstufen richtig um, Hilfslinie
  überlebt Speichern und Laden, Minimap-Klick verschiebt den Ausschnitt
  (24 Tests)

### 5.1 Was beim Bauen wichtig wurde

- [x] **Die Lineale liegen neben der Fläche, nicht darauf.** In den
  `paintEvent` gemalt wären sie weniger Arbeit gewesen, hätten aber die
  obersten und linkesten Zentimeter des Blatts unter sich begraben. Die
  Zeichenfläche steckt jetzt in einem Raster aus Ecke, oberem Lineal,
  linkem Lineal und Rollbereich
- [x] **Das Minimap-Abbild entsteht aus der Zeichenfläche selbst**
  (`render`), nicht aus einer zweiten Zeichenroutine. Jede Form sieht
  darin deshalb automatisch so aus wie im Diagramm - und bleibt es,
  wenn später ein Diagrammtyp dazukommt. Gerendert wird bei Zoom 1,0
  und danach zurückgestellt: sonst zeigte die Minimap beim
  Hineinzoomen immer weniger vom Diagramm, genau das Gegenteil dessen,
  wofür sie da ist
- [x] **Ein Fehler, den erst der Probelauf zeigte:** die Minimap blieb
  ein leerer weißer Kasten. Das Abbild entstand nur, wenn
  `minimap.isVisible()` wahr war - und solange das Fenster selbst noch
  nicht gezeigt wurde, meldet Qt dort `False`, obwohl `setVisible(True)`
  längst gelaufen war. Jetzt merkt sich das Fenster den Zustand selbst
- [x] `guides` ist im Schema ergänzt und **freiwillig**: jede `.pdiag`
  aus der Zeit davor bleibt gültig. Unsinn im Feld wird übergangen,
  statt den Editor mitzunehmen

## 6. Kleinigkeiten — erledigt

- [x] Eigener Eintrag für `NatterDatenError` in
  `docs/fehlerkatalog.yaml`. Bis dahin greift der `ValueError`-Eintrag,
  dessen Prüfe-Text bei Meldungen mit Komma einen Dezimalkomma-Hinweis
  enthält, der nicht immer passt (aus M10)
- [x] Abnahmetest für `matplotlib`-Diagramme aus Konsolenprogrammen per
  `plt.show()` in einem eigenen Fenster (aus M5). Es funktioniert
  bereits „umsonst", seit `matplotlib` Abhängigkeit ist – belegt ist es
  nicht
- [x] ~~Zugangsdaten für Datenbankverbindungen verschlüsselt im Windows
  Credential Store~~ – **entfällt.** Mit dem Wegfall von MySQL
  (Abschnitt 3.1) gibt es in Natter keine Zugangsdaten mehr, die
  abzulegen wären. Eine SQLite-Datei braucht keine
- [x] Ein Durchgang über Abstände und Ausrichtung in Objektinspektor,
  Explorer und Palette (letzter offener Punkt des visuellen
  Feinschliffs aus M1). Drei Funde, alle am Bildschirmfoto:
  - der Komponentenbaum rückte mit Qts 20 px ein, der Projekt-Explorer
    mit 14 – zwei Bäume, gleichzeitig im Fenster, verschieden tief.
    `EINRUECKUNG` steht jetzt an einer Stelle und gilt für beide
  - die Eigenschaften-Tabelle zeigte eine **Zeilennummern-Spalte**.
    Niemand spricht eine Eigenschaft als „Nummer 3" an, und in Lazarus'
    Objektinspektor steht dort nichts; die Spalte kostete nur Platz,
    ausgerechnet links vom Namen
  - die Wert-Spalte endete mitten im Dock, während der Wert darin
    abgeschnitten war. Sie füllt jetzt die Breite, wie im Explorer

## Zurückgestellt – bewusst und begründet

Nichts davon wird in M15 angefasst; hier steht, warum.

- **ER-Diagramm, Syntaxdiagramm, Import von DIA-Dateien** – im Konzept
  ausdrücklich „später möglich". Sieben Diagrammtypen decken den
  Unterricht ab
- **Mehrere Struktogramme auf einer Seite** – eine `.pdiag` führt genau
  ein `root`; mehrere würden durch Trefferprüfung, Quelltexterzeugung
  und Export durchschlagen. Wer zwei nebeneinander drucken will, legt
  zwei Dateien an
- **Echter MariaDB-Verbindungstest** – **hinfällig.** MySQL/MariaDB
  fällt mit Abschnitt 3.1 ganz weg; die Kontoverwaltungs-Abnahme läuft
  ohnehin gegen SQLite, und mehr braucht der Unterricht nicht
- **Update-Mechanismus und CI-Release-Automatisierung** – beide seit M8
  zurückgestellt. Auf verwalteten Schulrechnern verteilt die
  Systembetreuung, nicht das Programm sich selbst
- **Gekauftes Authenticode-Zertifikat** – nur nötig für die Verteilung
  an Rechner außerhalb der Schule. Das selbst erstellte Zertifikat ist
  am echten Smart App Control geprüft
