# Arbeitspaket M8 – Lazarus-Import, Exe-Export, Verteilung

Siehe konzept-natter.md, Abschnitt 15 (Lazarus-Import), 16 (Exe-Export),
17 (Verteilung: portabler Ordner ohne Installation). Kleinteilig wie
M1–M7 – aber mit einer wichtigen Einschränkung vorab.

## Abnahmekriterium (aus dem Konzept)

Ein Lazarus-Übungsprojekt importieren, fertigstellen, als Exe starten;
ZIP auf Rechner ohne Python entpacken und vollständig nutzen; veränderte
Datei wird erkannt.

## Umfang in dieser Entwicklungsumgebung

**Diese Sandbox ist kein Windows-Zielrechner.** Exe-Export (PyInstaller),
das portable ZIP-Paket, der Starter/Launcher, die Authenticode-Signatur
und der USB-Stick-Test aus Abschnitt 17 brauchen zwingend einen echten
Windows-Rechner (PyInstaller baut plattformspezifisch, Codesignatur
braucht ein Zertifikat, das nur der Nutzer anlegen kann, siehe Abschnitt
17.8: „privater Schlüssel … niemals im Repository“). Diese Teile von M8
bleiben deshalb hier unbearbeitet und sind unten unter „Zurückgestellt,
braucht einen echten Windows-Rechner“ einzeln aufgeführt statt sie zu
simulieren.

Bearbeitet wird der Teil, der reine Logik ist und sich gegen die bereits
vorhandenen `referenz/lazarus/*.lfm`-Dateien automatisiert testen lässt:
der `.lfm`-Parser und die Klassen-/Eigenschaftszuordnung nach `.pfm`.

### 1. `.lfm`-Parser — erledigt

- [x] `ide/import_lfm/parser.py`: `parse_lfm(text) -> dict` – zeilenbasierter
  rekursiver Parser für `object Name: Klasse … end` mit verschachtelten
  Kindobjekten, `Schlüssel = Wert`-Eigenschaftszeilen (auch
  `Font.Style`/`Brush.Color`-Pfade), einzeilige Mengen `[...]`,
  mehrzeilige mehrwertige Sammlungen `(...)` (z. B. `Items.Strings`),
  mehrzeilige Binärblöcke `{...}` (z. B. `Picture.Data`, als
  unangetasteter Rohtext behandelt statt geparst) – vorab an allen 19
  echten `.lfm`-Dateien aus `referenz/lazarus/` gegengeprüft (kleines
  Analyseskript, kein Teil des Repositorys), dass das reale Format
  keine mehrzeiligen Mengen oder String-Verkettungen enthält, bevor die
  Grammatik dafür festgelegt wurde
- [x] Tests (`tests/test_lfm_parser.py`, 27 Tests): gegen die echten
  `.lfm`-Dateien aus `referenz/lazarus/` geparst (u. a. `k_Ampel`,
  `g_StringGrid`, `f_Pizza`, `d_Cookie_klicker` wegen `Picture.Data`),
  Zahlen/Strings/Bezeichner/Mengen/Sammlungen/Binärblöcke einzeln
  geprüft, dazu alle 19 Referenzprojekte parametrisiert durchprobiert
  (parst jede ohne Absturz)

### 2. Klassen-/Eigenschaftszuordnung nach `.pfm` — erledigt

- [x] `ide/import_lfm/zuordnung.py`: `lfm_zu_pfm(geparstes_lfm) ->
  LfmImportErgebnis` (`.pfm`-`dict` + Liste von Warnungen für nicht
  unterstützte Komponenten/Eigenschaften, Abschnitt 15: „Nicht
  unterstützte Komponenten … im Importbericht aufgeführt“)
- [x] Klassenzuordnung (`TButton → Button` usw., siehe `_KLASSEN` in der
  Implementierung) für alle in `referenz/lazarus/` tatsächlich
  verwendeten Komponententypen; unbekannte Klassen landen als Warnung im
  Importbericht statt einen Platzhalter anzulegen – **Vereinfachung,
  bewusst dokumentiert:** es gibt noch keine generische
  „Platzhalter“-Komponente in `pcl`, das Anlegen echter Platzhalter wäre
  ein eigenes Feature
- [x] Eigenschaftszuordnung (`Caption → caption`, `Left/Top/Width/Height`,
  `Checked`, `Text`, `ReadOnly`, `ItemIndex`, `RowCount`/`ColCount`,
  `Color`/`Brush.Color`, `Shape`) für die in `referenz/lazarus/`
  tatsächlich verwendeten Eigenschaften; `Form.left`/`.top` werden
  verworfen (Formulare haben in `pcl` keine solche Prop)
- [x] Farben: `clXxx`-Konstanten (die im Referenzmaterial tatsächlich
  vorkommenden: `clBlack`, `clGray`, `clSilver`, `clYellow`, dazu eine
  größere Standardtabelle für den Rest) sowie `$00BBGGRR`-Hex (Lazarus-
  Byte-Reihenfolge, nicht `$RRGGBB`) → `#RRGGBB`
- [x] Ereignisse: `OnClick = b_startClick` → `on_click`-Ereignis mit dem
  Python-Methodennamen `b_start_click` (Suffix wie `Click`/`Change`/
  `Create` abtrennen, Rest in snake_case – deckt sich mit der
  tatsächlichen Methodenbenennung in den bestehenden
  `beispielprojekte/*/u_main.py`, z. B. `form_create` für `FormCreate`)
- **Bekannte Lücke, dabei entdeckt (kein Teil dieses Arbeitspakets):**
  `ComboBox`/`ListBox`/`Memo`-`items`-Sammlungen (`Strings`) lassen sich
  über das `.pfm`-Format bisher überhaupt nicht abbilden – `items` ist
  eine reine Python-`@property`, kein `Prop`, und taucht deshalb weder in
  `pfm_aus_formular()` noch im `.pfm`→Python-Generator auf. Das betrifft
  nicht nur den Lazarus-Import, sondern auch den Designer selbst (M3).
  `Items.Strings`/`Cells`-Eigenschaften landen deshalb hier bewusst als
  „nicht unterstützt“ im Importbericht statt sie unvollständig zu
  simulieren; ein Fix gehört in die `.pfm`-Pipeline selbst, nicht in den
  Lazarus-Importer
- [x] Tests (`tests/test_lfm_zuordnung.py`, 19 Tests): Klassen-/
  Eigenschafts-/Farb-/Ereigniszuordnung einzeln geprüft, dazu ein
  Ende-zu-Ende-Test gegen das echte `k_Ampel`-`.lfm` – **Korrektur
  gegenüber dem ursprünglichen Plan:** das Ergebnis wird gegen
  `schemas/pfm.schema.json` validiert, aber bewusst *nicht* mit der von
  Hand gepflegten `beispielprojekte/Ampel/u_main.pfm` verglichen – die
  hat abweichende, bewusst umbenannte Komponentennamen (`Label1` →
  `l_titel`, `Shape1` → `s_gehaeuse`, `b_Auschalten` → `b_auschalten`),
  während ein echter Import die Original-Lazarus-Namen unverändert
  übernehmen muss (Abschnitt 15: der Design-Prüfer aus M7 soll genau
  solche Namen danach als Hinweis aufgreifen, nicht der Importer selbst
  sie stillschweigend „reparieren“); stattdessen wird strukturell
  geprüft (Kinderanzahl, bekannte Eigenschaftswerte, `OnChangeBounds`
  bei `s_rot` taucht korrekt im Importbericht auf, da nicht unterstützt)

### 3. IDE-Verdrahtung — erledigt; Pascal-Rumpf-Übernahme, Bild-Extraktion — zurückgestellt

- [x] „Werkzeuge → Lazarus-Formular importieren …“ (Menüaktion: `.lfm`
  wählen, Ziel-`.pfm` wählen, ruft Schritt 1+2 auf, schreibt die `.pfm`,
  öffnet den Designer, läuft direkt durch den Design-Prüfer aus M7 –
  Lazarus-Import-Hinweise und Design-Prüfer-Funde erscheinen gemeinsam
  im Panel „Meldungen“, mit `[Lazarus-Import]`-Präfix unterscheidbar)
- [x] Tests (`tests/test_hauptfenster_lazarus_import.py`, 5 Tests): Import
  erzeugt ein gültiges `.pfm` und öffnet den Designer, Importbericht für
  nicht unterstützte Komponenten erscheint, Abbruch bei Quelle/Ziel tut
  nichts, ungültiges `.lfm` zeigt eine Fehlermeldung statt abzustürzen
- **Per Screenshot visuell geprüft, gegen das echte `k_Ampel/u_main.lfm`
  (nicht nur ein Test-Fixture):** rendert korrekt im Designer (Label,
  graues Gehäuse-Shape sichtbar), 13 Hinweise im Importbericht (u. a.
  `ClientHeight`/`LCLVersion`/`TabOrder` – Eigenschaften ohne
  `pcl`-Entsprechung), Design-Prüfer-Funde (fehlendes 8px-Raster,
  Überlappungen der Shapes) erscheinen im selben Panel
- [ ] Aus der zugehörigen `.pas` den Pascal-Rumpf jedes Ereignis-Handlers
  als Kommentar in die neu angelegte Python-Methode übernehmen (braucht
  einen eigenen, einfachen `.pas`-Methodenrumpf-Extraktor) –
  zurückgestellt
- [ ] Bilder aus `Picture.Data` (siehe Parser oben) nach `assets/`
  extrahieren (das Lazarus-eigene Container-Format um die eigentlichen
  Bilddaten muss zuerst verstanden werden – bisher nur als Rohtext
  durchgereicht, nicht dekodiert) – zurückgestellt

### 4. Exe-Export, Verteilung — PyInstaller-Kern erledigt, Signatur weiterhin zurückgestellt

Jetzt auf einem echten Windows-Rechner bearbeitet (September 2026,
Nutzer-Feedback: „Wie funktioniert es, dass die Exe erstellt wird –
das soll auch gebaut werden“).

- [x] PyInstaller-Exportpipeline (Abschnitt 16): `ide/export/exporter.py`
  (`exe_exportieren`), verdrahtet als „Projekt → Als Exe exportieren …“.
  Ordner-Variante (kein `--onefile`, wie in `prototypes/s5_pyinstaller`
  empfohlen), `--windowed` nur für `gui`/`gui_db`-Projekte, `--icon` aus
  den `export`-Einstellungen der `.natter`-Datei
  (`schemas/project.schema.json`), Zwischenstände (`build/`, `.spec`)
  werden nach dem Bau wieder entfernt.
- [x] **Echter Bau + echter Probestart** (nicht nur simuliert): Ampel
  aus `beispielprojekte/` in einen temporären Ordner kopiert, mit
  `exe_exportieren` gebaut (~360-390s für ein PySide6-Projekt dieser
  Größe), das Ergebnis-ZIP in einen komplett neuen Ordner entpackt und
  `Ampel.exe` von dort gestartet.
  - **Beim ersten Versuch abgestürzt**: „Unhandled exception in script“
    – `[Errno 2] No such file or directory:
    '...\_internal\design\tokens.json'`. Ursache: `pcl/theme/__init__.py`
    liest `design/tokens.json` zur Laufzeit über einen relativen Pfad
    drei Ebenen über der Datei; PyInstaller bindet aber nur den
    Python-Code automatisch ein, keine beliebigen Daten-Dateien. Das
    betrifft **jedes** `pcl`-Programm, nicht nur Ampel, da
    `pcl.theme._tokens_laden()` von so gut wie jeder `Form` benutzt
    wird.
  - **Behoben** an der Wurzel: `pcl/theme/__init__.py` erkennt jetzt
    `sys._MEIPASS` (von PyInstaller gesetzt) und sucht dort statt am
    Quellcode-relativen Pfad; der Exporter bindet `design/` zusätzlich
    über `--add-data` ein. Beide Seiten haben eigene, echte Tests
    (`tests/test_theme.py`, `tests/test_exporter.py`).
  - **Zweiter Bau, nach dem Fix**: Fenstertitel der gestarteten Exe ist
    „Ampel“ (vorher „Unhandled exception in script“) – die echte
    Ampel-Oberfläche läuft eigenständig, ganz ohne die Entwicklungs-
    Python-Umgebung im Pfad. Als Abnahme-Nachweis: Prozess läuft,
    reagiert (`Responding=True`), zeigt den korrekten Fenstertitel.
- [x] `pyinstaller>=6.11` als reguläre Abhängigkeit in `pyproject.toml`
  (Teil derselben portablen Python-Umgebung wie die IDE selbst, siehe
  `prototypes/s1_portables_python` – kein separater Download auf dem
  Schüler-Rechner nötig).
- [ ] Portables ZIP-Paket mit signiertem Prüfsummen-Manifest,
  Authenticode-Signatur (Abschnitt 17) – `prototypes/s6_signatur` steht
  bereit, aber bewusst nicht automatisch eingebunden: eine
  Authenticode-Signatur braucht ein gekauftes Zertifikat, das ein
  Schulprojekt normalerweise nicht besitzt (Abschnitt 17.8: „privater
  Schlüssel … niemals im Repository“). Ein einfaches ZIP ohne Signatur
  wird bereits erzeugt (`export.target` in der `.natter`-Datei).
- [ ] Test „veränderte Datei im Paket wird beim Start erkannt“
  (Prüfsummen-Manifest, hängt am obigen Punkt) – zurückgestellt
- [ ] Vollständige Abnahme mit einem **echten, per Lazarus-Import
  fertiggestellten** Übungsprojekt (bisher nur mit dem vorhandenen
  Ampel-Beispielprojekt geprüft) – zurückgestellt
