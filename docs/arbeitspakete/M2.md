# Arbeitspaket M2 – IDE-Grundgerüst

Siehe konzept-natter.md, Abschnitt 20, 7.1–7.5, 7.9. Kleinteilig
aufgeschlüsselt wie M1 in `docs/PLAN.md`. Jeder Schritt einzeln mit
`pytest`/`pytest-qt` headless testbar, wo möglich.

## Abnahmekriterium (aus dem Konzept)

Projekt aus M1 in der IDE öffnen, alle Datei-Menüfunktionen funktionieren,
Projekt `u_pflanzen`/`u_garten` anlegen und starten, Konsolenprogramm mit
`input()` im eigenen Fenster.

## Reihenfolge

Zuerst das, was der Projektinhaber konkret nachgefragt hat (Neues
Projekt/Projekt öffnen) und headless testbar ist; Monaco, die portable
Laufzeit und die "eigenen Fenster" für GUI/Konsole sind Windows-lastig
bzw. laut Abschnitt 21 riskant und kommen später (siehe `prototypes/`).

### 1. Hauptfenster-Grundgerüst (`ide/shell/`) — erledigt

- [x] `HauptFenster(QMainWindow)`: Menüleiste mit den 11 Menütiteln aus
  Abschnitt 7.2 (noch ohne Einträge), zentrale Editor-Tabs
  (schließbar/verschiebbar), Docks für Projekt-Explorer (links),
  Objektinspektor (rechts), Panels mit den vier Reitern aus Abschnitt
  7.1 (unten), Statusleiste
- [x] Tests (`tests/test_hauptfenster.py`, 7 Tests)

### 2. Aktionsregister (`ide/actions/`) — erledigt

- [x] `Aktion`: ID, deutscher Name, Menüposition, Tastenkürzel, Bereich,
  Symbolname (wie `docs/aktionen.md`-Vorlage), erzeugt einmal ein
  `QAction`, das für Menü/Werkzeugleiste/Befehlspalette weiterverwendet
  wird statt mehrfach erzeugt zu werden
- [x] `Aktionsregister`: `registrieren()` (lehnt doppelte IDs und
  Tastenkürzel-Konflikte ab, Abschnitt 7.9), `an_hauptfenster_anhaengen()`
  fügt jede Aktion in ihr Menü ein
- [x] Tests (`tests/test_aktionsregister.py`, 7 Tests)

### 3. Projektmodell (`ide/project/`) — erledigt

- [x] `Projekt.laden()`: `.natter`-Datei oder ihr Ordner, validiert gegen
  `schemas/project.schema.json`; `speichern()`; `units()`/`formulare()`
  aus dem Ordnerinhalt ermittelt (nicht nur aus der `.natter`, da Units
  beim Einbinden nicht einzeln eingetragen werden, Abschnitt 4.1)
- [x] Tests (`tests/test_projekt.py`, 8 Tests) gegen
  `beispielprojekte/Ampel/` geprüft

### 4. Neu-Dialog: „Neues Projekt …“ (`ide/project/`, `templates/`) — erledigt

- [x] `templates/gui/` (Minimalvorlage: `main.py`, leere `u_main.pfm`,
  `u_main.py`-Startdatei, `<name>.natter`) und `templates/console/`
  (Abschnitt 7.5); `gui_db/` folgt mit den SQLdb-Komponenten (M5)
- [x] `ide/project/neu.py`: `projekt_erzeugen(vorlage, zielordner, name)`
  rendert die Vorlage, erzeugt `u_main_design.py` über
  `ide.codegen.design`, lehnt unbekannte Vorlagen und nicht-leere
  Zielordner ab
- [x] Tests (`tests/test_projekt_neu.py`, 5 Tests): erzeugtes
  GUI-Projekt lässt sich wie ein Abnahmeprojekt instanzieren und zeigt
  den gewählten Namen als Fenstertitel

### 5–7. „Projekt öffnen …“/„Öffnen …“, Projekt-Explorer, Editor-Tab — erledigt (zusammen umgesetzt)

Die drei Schritte hängen eng zusammen (Projekt öffnen ergibt nur mit
Explorer Sinn, Doppelklick im Explorer braucht einen Editor-Tab) und
wurden daher in einem Arbeitsschritt umgesetzt:

- [x] `ide/shell/explorer.py`: `ProjektExplorer(QTreeWidget)`, Gruppen
  Formulare/Units (Abschnitt 7.4); Formular-Units erscheinen nur bei
  Formulare, nicht doppelt bei Units. `Assets`-Gruppe folgt, sobald
  Bild-/Sound-Komponenten das brauchen
- [x] `HauptFenster.projekt_oeffnen()`: lädt über `Projekt.laden()`,
  füllt den Explorer; Aktion „Projekt öffnen …“ (Projekt-Menü) fragt
  über `QFileDialog` nach der `.natter`-Datei
- [x] `HauptFenster.datei_oeffnen()`: öffnet eine Datei in einem
  `QPlainTextEdit`-Tab (Platzhalter, siehe unten), aktiviert einen
  bereits offenen Tab statt ihn doppelt zu öffnen; Aktion „Öffnen …“
  (Strg+O) im Datei-Menü
- [x] Doppelklick im Explorer öffnet die zugehörige Datei
  (`itemDoubleClicked` → `datei_oeffnen`)
- [x] Änderungsmarkierung `●` im Tab-Titel
  (`QTextDocument.modificationChanged`) und Aktion „Speichern“ (Strg+S)
  schreibt die Datei und setzt die Markierung zurück
- [x] Monaco ersetzt den `QPlainTextEdit`-Platzhalter in einem eigenen
  späteren Schritt, sobald S2 (`prototypes/s2_monaco_jedi/`) auf dem
  Windows-Laptop bestätigt ist (Abschnitt 21: höchstes Risiko im
  Konzept) – bis dahin voll benutzbar, nur ohne Hervorhebung/
  Vervollständigung
- [x] Tests: `tests/test_explorer.py` (3), `tests/test_hauptfenster.py`
  (+1), `tests/test_hauptfenster_projekt.py` (6) – u. a. gegen
  `beispielprojekte/Ampel/` geprüft, echte Änderung+Speichern-Runde
  über `insertPlainText`/`_aktuelle_datei_speichern`

### 8. „Unit öffnen …“ (Schnellauswahl) — erledigt

- [x] `ide/shell/schnellauswahl.py`: `SchnellAuswahl`-Dialog mit
  Suchfeld (filtert live, alphabetisch sortiert) und Liste; Enter im
  Suchfeld übernimmt die erste Zeile, Aktivierung eines Eintrags auch
  (Abschnitt 7.4, 7.9); Aktion „Unit öffnen …“ (Strg+P) im Datei-Menü
- [x] Tests (`tests/test_schnellauswahl.py`, 5 Tests) – Filterlogik ohne
  `exec()` getestet (siehe Moduldocstring), nur der eigentliche modale
  Aufruf würde blockieren

### 9. „Neue Unit“ und das u_pflanzen/u_garten-Beispielprojekt — erledigt

- [x] `HauptFenster.unit_erzeugen()`: legt `u_neu<n>.py` an (erste freie
  Nummer) oder mit gegebenem Namen, lehnt vorhandene Dateien und „kein
  Projekt offen“ ab, aktualisiert den Explorer, öffnet die neue Datei;
  Aktion „Neue Unit“ (Strg+N) im Datei-Menü
- [x] Tests (`tests/test_neue_unit.py`, 6 Tests)
- [x] `beispielprojekte/Garten/`: das im Konzept (Abschnitt 7.4)
  genannte Mehr-Unit-Beispiel `u_pflanzen.py`/`u_garten.py`/`u_main.py`
  wortgleich zu den Codebeispielen im Konzept, dazu ein kleines Formular
  (Name/Wasserbedarf eingeben, „Pflanzen“ pflanzt, Liste zeigt den
  Garten)
- [x] Tests (`tests/test_beispiel_garten.py`, 4 Tests): Formularlogik
  headless über echte Klicks **und** das Projekt in der IDE öffnen –
  Explorer gruppiert `u_main` korrekt bei Formulare, `u_pflanzen`/
  `u_garten` bei Units

**M2, Schritte 1–9 damit erledigt** (158 Tests grün) – das
M2-Abnahmekriterium aus dem Konzept ist damit bis auf die
Windows-spezifische „Ausführung in eigenen Fenstern“ funktional erfüllt.
Weiter mit den zurückgestellten Punkten, sobald sinnvoll priorisiert
(siehe unten) – `docs/PLAN.md` „Wo wir stehen“ nennt den jeweils
nächsten.

## Zurückgestellt innerhalb M2 (folgen, nachdem 1–9 stehen)

- Monaco-Einbindung (ersetzt Schritt 7), Jedi-Vervollständigung, Ruff-
  Markierung im Editor
- vollständiges Menü/Werkzeugleisten-Set aus Abschnitt 7.2/7.3, SVG-
  Symbole
- Units-Feinschliff (Einbinden-Dialog mit automatischem Import-Einfügen,
  Umbenennen mit Importanpassung, Kreisbezug-Erkennung, geteilte
  Ansicht, Tabs anheften/Drag&Drop) – „Neue Unit“ und das grundlegende
  Mehrere-Units-Projekt sind mit Schritt 9 bereits abgedeckt
- Sitzung (`.natter-session`): offene Tabs/Cursor/Breakpoints merken
- portable Laufzeit mit getrennten Paketordnern (braucht S1/S4 auf dem
  Windows-Laptop bestätigt)
- Ausführung in eigenen Fenstern (GUI + Konsole) – Konsolenfenster ist
  Windows-spezifisch, hier headless nur eingeschränkt testbar
- Tastenkürzel-Tab, Befehlspalette
- `u_pflanzen`/`u_garten`-Mehr-Unit-Beispielprojekt für die M2-Abnahme
