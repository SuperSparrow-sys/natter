# Arbeitspaket M5 – Datenbank, pandas, Charts

Siehe konzept-natter.md, Abschnitt 10 (Datenbankanbindung), 11
(Dateien, Bilder, HTML und Datenauswertung), 18 (Repository-Struktur:
`pcl/db/`, `pcl/components/data_access.py`, `data_controls.py`,
`chart.py`, `pcl/dataframe.py`, `pcl/files.py`, `ide/viewers/`,
`ide/database/`). Kleinteilig wie M1–M4.

## Abnahmekriterium (aus dem Konzept)

Kontoverwaltung (`referenz/lazarus/n_konto`) mit MariaDB/SQLite; CSV-
Auswertung mit pandas in StringGrid und Chart; Würfelspiel-Highscore als
HTML im Browser.

## Reihenfolge

SQLdb-Kern zuerst (reine Python-Logik über `sqlite3`, keine Qt-, keine
Netzwerkabhängigkeit – nach demselben Muster wie `pcl/properties.py` in
M1), weil Data Controls, DB-Panel und die Kontoverwaltungs-Abnahme
darauf aufbauen. MySQL-Unterstützung (`PyMySQLConnection`) direkt danach,
aber mit einer wichtigen Einschränkung (siehe „Stolperstein MariaDB“
unten). Danach Data Controls (Qt-Widgets, brauchen SQLdb). Parallel dazu,
weitgehend unabhängig: `pandas`-Anbindung (`pcl/dataframe.py`) und die
Chart-Komponente (`matplotlib`) – beide brauchen keine Datenbank. Dann
`open_url` (seit M1 zurückgestellt, jetzt naheliegend, weil die
Würfelspiel-HTML-Abnahme es braucht) und die IDE-Betrachter (CSV/Bild/
HTML). DB-Panel zuletzt vor der Komponentenkette, weil es SQLdb, Data
Controls und die CSV-Betrachter-Logik wiederverwendet. Abnahme ganz am
Schluss.

**Stolperstein MariaDB, vorab festgehalten:** Abschnitt 19
(Teststrategie) sieht für die Datenbankebene ausdrücklich zwei Stufen
vor – „SQLite in-memory; danach MariaDB im Docker-Container auf dem
Homeserver“. Der Homeserver-Container existiert in dieser
Entwicklungsumgebung nicht. `MySQLConnection` wird deshalb vollständig
gegen die echte `PyMySQL`-Bibliothek implementiert (Parameter-
Übersetzung, Verbindungsaufbau, Feldtypen) und mit echten, aber
lokal simulierten Antworten getestet, wo möglich; ein echter Verbindungs-
/Transaktionstest gegen eine laufende MariaDB-Instanz bleibt offen und
wird als eigener Punkt in „Zurückgestellt“ geführt, bis der Homeserver
verfügbar ist. Die Kontoverwaltungs-Abnahme läuft deshalb zunächst nur
gegen SQLite; der MariaDB-Teil der Abnahme wird nachgeholt.

### 1. SQLdb-Kern gegen SQLite — erledigt

- [x] `pcl/db/__init__.py`: `uebersetze_platzhalter(sql, stil)` –
  `"named"` (sqlite3) lässt den Text unverändert, `"pyformat"` (PyMySQL,
  Schritt 2) ersetzt `:name` durch `%(name)s`, rein regex-basiert ohne
  Verbindung
- [x] `pcl/components/data_access.py`: `SQLite3Connection`
  (`database_name` Prop = Dateipfad oder `:memory:`, `connected`-Prop
  öffnet/schließt), `SQLTransaction` (`commit()`/`rollback()`),
  `SQLQuery` (`sql`-Prop, `params` als einfaches dict, `open()` für
  SELECT, `exec_sql()` für INSERT/UPDATE/DELETE, `eof`, `next()`,
  `field_by_name(name).as_string`/`.as_integer`/`.as_float`, `close()`),
  `DataSource` (`dataset` verweist auf eine `SQLQuery`, wird von Data
  Controls in Schritt 5 gelesen)
- [x] Fehlerkatalog-Anbindung: `pcl/errors.py` mit `NatterDatenbankError`
  (Abschnitt 8.5: „Datenbankverbindung, SQL-Fehler“) statt der rohen
  `sqlite3`-Ausnahme; SQL-Fehler und fehlgeschlagene Verbindungen lösen
  sie aus
- [x] Tests (`tests/test_db_sqlite.py`, 17 Tests): `:memory:`-Datenbank
  anlegen, Tabelle erzeugen, `SELECT`/`INSERT`/`UPDATE`/`DELETE` über
  `SQLQuery`, benannte Parameter (inkl. eines SQL-Injection-Versuchs, der
  dank Parameterbindung als reiner Text behandelt wird), `eof`/`next()`-
  Durchlauf, Transaktion mit `commit()`/`rollback()`, SQL-Fehler löst
  `NatterDatenbankError` statt roher `sqlite3.OperationalError` aus,
  Platzhalter-Übersetzung isoliert getestet
- **Vereinfachung, bewusst dokumentiert:** Objektverweise zwischen
  Komponenten (`SQLQuery.database`, `SQLTransaction.database`,
  `DataSource.dataset`) sind einfache Instanzattribute statt `Prop`, weil
  `Prop` nur Wertetypen mit sinnvollem Standardwert kennt; eine Bindung
  im Objektinspektor per Dropdown ist erst mit den Data Controls in
  Schritt 5 nötig und wird dort eigens gelöst

### 2. SQLdb-Kern für MySQL/MariaDB — erledigt

- [x] `pcl/components/data_access.py`: gemeinsame Basis
  `_Datenbankverbindung` aus Schritt 1 herausgezogen (Verbindung öffnen/
  schließen über `connected`, `_platzhalterstil`/`_treiber_fehler` als
  Vertrag für `SQLQuery`); `MySQLConnection` (`host_name`/`port`/
  `database_name`/`user_name`/`password`, `connected`) auf `PyMySQL`,
  `SQLite3Connection` unverändert in ihrem Verhalten auf dieselbe Basis
  umgestellt
- [x] `SQLQuery._ausfuehren()` treiberunabhängig gemacht: liest
  Platzhalterstil und Treiberausnahme von der jeweils verbundenen
  `database`-Instanz, statt `sqlite3` fest zu verdrahten
- [x] Parameter-Übersetzung `:name` → `%(name)s` (PyMySQL-Stil) bereits
  in Schritt 1 in `pcl/db/` angelegt, hier gegen `MySQLConnection`
  verdrahtet
- [x] `PyMySQL`, `SQLAlchemy` (für `pandas.read_sql` mit MySQL, Abschnitt
  11.6) als echte Laufzeit-Abhängigkeiten in `pyproject.toml`
  (`dependencies`, nicht mehr nur `prototypes`)
- [x] Tests (`tests/test_db_mysql.py`, 4 Tests): Parameter-Übersetzung
  isoliert (keine Verbindung nötig), `SQLQuery` gegen `MySQLConnection`
  übersetzt vor der Ausführung korrekt, Verbindungsfehler (nicht
  erreichbarer Host) liefert `NatterDatenbankError` statt rohem
  `PyMySQL`-Traceback, `connected` bleibt nach Fehlschlag `False`
- [x] **Zurückgestellt, siehe „Stolperstein MariaDB“ oben:** echter
  Verbindungs-/Query-/Transaktionstest gegen eine laufende MariaDB-
  Instanz (Homeserver-Docker-Container existiert hier nicht) – bleibt in
  „Zurückgestellt“ unten eingetragen

### 3. `pandas`-Anbindung — erledigt

- [x] `pandas`, `numpy`, `openpyxl` als echte Laufzeit-Abhängigkeiten
  (`pandas` war bisher nur in der `prototypes`-Gruppe, jetzt entfernt
  dort und in `dependencies` verschoben)
- [x] `pcl/dataframe.py`: `load_dataframe(grid, df)`/`to_dataframe(grid)`
  als freie Funktionen (pandas-Import bleibt lokal in der Funktion, `pcl`
  selbst bleibt ohne pandas-Abhängigkeit beim bloßen Importieren);
  `StringGrid.load_dataframe(df)`/`.to_dataframe()` in
  `pcl/components/additional.py` delegieren nur dorthin
- [x] `SQLQuery.to_dataframe()`: führt `sql` aus und liefert das
  vollständige Ergebnis als `DataFrame` – **vereinfacht gegenüber dem
  ursprünglichen Plan:** direkt über den eigenen, bereits getesteten
  Cursor-Mechanismus (`fetchall()` + Spaltennamen) statt über ein
  separates `.engine`/`pandas.read_sql()`. Ein `.engine`
  (SQLAlchemy) hätte für `:memory:`-SQLite eine eigene Lösung gebraucht
  (jede neue SQLAlchemy-Verbindung zu `sqlite:///:memory:` wäre eine
  *andere*, leere Datenbank als die über `SQLite3Connection` bereits
  offene) – der direkte Cursor-Weg ist einfacher und robuster und
  erfüllt dieselbe Anforderung aus Schülersicht (`self.query.to_dataframe()`)
- [x] Tests (`tests/test_dataframe.py`, 6 Tests): `DataFrame` mit
  gemischten Spaltentypen (Text/Zahl/Wahrheitswert) → `load_dataframe` →
  `to_dataframe` ergibt auf Textebene denselben Inhalt, fehlende Werte
  (`None`) werden als Leerstring angezeigt, leerer `DataFrame` ergibt nur
  die Kopfzeile, `SQLQuery.to_dataframe()` gegen `:memory:`-SQLite

### 4. Chart-Komponente (`matplotlib`) — erledigt

- [x] `matplotlib` als echte Laufzeit-Abhängigkeit
- [x] `pcl/components/chart.py`: `Chart(Control)` bettet
  `FigureCanvasQTAgg` ein; `add_bar_series`, `add_line_series`,
  `add_pie_series`, `add_scatter_series`, `clear()` – nehmen Listen oder
  `pandas.Series` entgegen (matplotlib versteht beide Formen direkt)
- [x] Farben aus `design/tokens.json` (Abschnitt 11.6: „Farben aus dem
  Theme“) – **Vereinfachung, bewusst dokumentiert:** die Tokens-Datei
  kennt bisher keine eigene, größere Diagrammpalette, deshalb wird die
  vorhandene Dreierpalette Accent/Success/Danger für Serien
  wiederverwendet statt neuer, nicht freigegebener Tokens; ein Diagramm
  färbt sich einmalig beim Erzeugen nach dem aktuellen Theme ein, ein
  späterer Theme-Wechsel zur Laufzeit wirkt nicht automatisch auf bereits
  gezeichnete Diagramme zurück (wie bei keiner anderen `pcl`-Komponente
  mit eigenem Zeichen-Backend)
- [x] Tests (`tests/test_chart.py`, 7 Tests, headless): jede Serienart
  erzeugt eine sichtbare Achse mit der erwarteten Anzahl Datenpunkte
  (Balken/Torte über `patches`, Linie über `lines`, Punktwolke über
  `collections`), `clear()` entfernt alle Serien, `add_bar_series` nimmt
  eine echte `pandas.Series` entgegen, Theme-Wechsel ändert die
  Diagrammfarben
- [x] Referenz-Screenshots hell/dunkel (Abschnitt 19: „pandas/Chart“) –
  siehe Abnahme-Zwischenstand unten

### 5. Data Controls — erledigt

- [x] `SQLQuery` von Schritt 1 auf gepufferte Navigation umgestellt:
  `open()` liest jetzt alle Zeilen auf einmal (`fetchall()` statt
  `fetchone()`), dazu `first()`/`prior()`/`last()` neben dem bereits
  vorhandenen `next()`, sowie `record_count`/`record_index`/
  `column_names`/`all_rows()`/`set_field()` – rückwärtskompatibel zu
  Schritt 1 (`next()`/`eof`/`field_by_name()` verhalten sich für
  Vorwärts-Durchläufe exakt wie zuvor, alle 24 bisherigen SQLdb-/
  pandas-Tests bleiben unverändert grün)
- [x] `DataSource` bekommt `aktualisieren()`/eine interne
  Listener-Liste: Data Controls registrieren sich beim Erzeugen und
  zeichnen sich neu, sobald `aktualisieren()` aufgerufen wird
- [x] `pcl/components/data_controls.py`: `DBGrid` (zeigt alle Zeilen
  einer `DataSource` – **Vereinfachung gegenüber dem ursprünglichen
  Plan:** `QTableWidget` statt `QTableView` + eigenem Model, konsistent
  mit `StringGrid`s bereits etabliertem Muster, nicht neu erfunden),
  `DBEdit`/`DBText` (an ein Feld der `DataSource` gebunden, `DBEdit`
  schreibt Änderungen über `SQLQuery.set_field()` in den Zwischen-
  speicher zurück), `DBNavigator` (Erster/Zurück/Vor/Letzter bewegen den
  Datensatzzeiger direkt und benachrichtigen die `DataSource`;
  Einfügen/Löschen/Speichern/Abbrechen lösen `on_insert`/`on_delete`/
  `on_save`/`on_cancel` aus, siehe „Vereinfachung, bewusst dokumentiert“
  unten), `DBComboBox` (Auswahlliste aus einer Lookup-`DataSource`)
- **Vereinfachung, bewusst dokumentiert:** anders als Lazarus, wo der
  Dataset bei Einfügen/Löschen/Speichern automatisch die passende SQL-
  Anweisung erzeugt, löst `DBNavigator` dafür nur Ereignisse aus – der
  Kurs schreibt SQL immer selbst (Abschnitt 10.1 zeigt das schon am
  Beispielcode für SELECT/INSERT/UPDATE/DELETE), automatisch erzeugte
  SQL-Anweisungen im Hintergrund würden diesem Lernziel widersprechen.
  Ebenso ist die Benachrichtigung gebundener Controls über
  `DataSource.aktualisieren()` bewusst explizit statt (wie in Lazarus)
  automatisch bei jeder Dataset-Änderung
- [ ] Designzeit-Aktivierung (`SQLite3Connection.connected = True` im
  Inspektor zeigt echte Daten im `DBGrid` des Designers, Abschnitt 10.1)
  – **zurückgestellt:** braucht die Designer-/Inspektor-Integration aus
  M3 für nicht-visuelle Komponenten, die für SQLdb-Komponenten bisher
  nicht existiert; siehe „Zurückgestellt“ unten
- [x] Tests (`tests/test_data_controls.py`, 10 Tests, headless, echte
  `:memory:`-SQLite-Verbindung mit drei Kunden-Datensätzen): `DBGrid`
  zeigt alle Zeilen/Spalten, `DBEdit`/`DBText` zeigen das Feld der
  aktuellen Zeile, `DBNavigator`-Klicks (Vor/Zurück/Letzter) bewegen den
  Datensatzzeiger und aktualisieren gebundene Controls, Klemmen an den
  Rändern (kein Sprung vor die erste/nach der letzten Zeile), Einfügen/
  Löschen/Speichern/Abbrechen lösen ihre Ereignisse aus, `DBEdit`-
  Eingabe ändert das Feld im Query-Puffer (und wirkt nach erneuter
  Benachrichtigung auch im `DBGrid`), `DBComboBox` zeigt die Lookup-
  Spalte, `DBGrid` ohne offene Abfrage bleibt leer statt abzustürzen

### 6. `open_url` und Datei-/HTML-Grundlagen — noch offen

- [ ] `pcl/files.py`: `open_url(pfad_oder_adresse)` – löst relative Pfade
  zum Arbeitsverzeichnis auf (wie `LCLIntf.OpenURL`), nutzt
  `webbrowser.open` für `http(s)://`-Adressen und lokale Dateien
  gleichermaßen (Abschnitt 11.3)
- [ ] `pcl/__init__.py` exportiert `open_url`
- [ ] Tests (`tests/test_files.py`): relative/absolute Pfadauflösung
  gegen ein temporäres Arbeitsverzeichnis, `webbrowser.open` gemockt
  (Abschnitt 19: „`open_url` mit gemocktem Browser“) statt echten
  Browser zu öffnen

### 7. IDE-Betrachter (`ide/viewers/`) — noch offen

- [ ] CSV-Tabellenansicht: Trennzeichen- und Zeichensatz-Erkennung
  (Semikolon/Komma, UTF-8/Windows-1252, Dezimalkomma bei Excel-Export,
  Abschnitt 11.5), Umschalten Tabelle/Text, Sortieren/Filtern nur in der
  Ansicht, Datei bleibt unverändert
- [ ] Bildvorschau: Größe/Abmessungen anzeigen (Abschnitt 11.4)
- [ ] HTML-Vorschau: Vorschau-Tab mit automatischer Aktualisierung beim
  Speichern, „Im Browser öffnen“ per Menü/Rechtsklick (nutzt `open_url`)
- [ ] `HauptFenster`: `.csv`/Bilddateien/`.html` im Explorer doppelt
  angeklickt öffnen den passenden Betrachter statt (bzw. zusätzlich zu)
  des Quelltexteditors
- [ ] Tests (`tests/test_viewer_csv.py`, `tests/test_viewer_html.py`):
  Excel-CSV (`;`, Windows-1252, Dezimalkomma) korrekt erkannt und
  angezeigt, UTF-8-CSV mit Komma ebenso; HTML-Vorschau zeigt
  gespeicherten Inhalt, „Im Browser öffnen“ ruft `open_url` mit dem
  richtigen Pfad auf (gemockt)

### 8. Datenbank-Panel in der IDE (`ide/database/`) — noch offen

- [ ] Verbindung testen (SQLite-Datei oder MySQL-Zugangsdaten),
  Tabellen-/Spaltenbaum anzeigen, SQL-Abfrage-Eingabe mit Ergebnis als
  Tabelle (Abschnitt 10.2)
- [ ] „Werkzeuge → CSV in Datenbank importieren …“: liest eine CSV-Datei
  (wiederverwendet die Erkennung aus Schritt 7) in eine neue Roh-/
  Importtabelle
- [ ] Export einer Tabelle als CSV und als SQL-Dump im Datenbank-Panel
- [ ] Zugangsdaten-Speicherung: **vereinfacht gegenüber Abschnitt 10.2**
  für den MVP nur für die laufende Sitzung im Speicher gehalten, nicht
  dauerhaft im Windows Credential Store verschlüsselt abgelegt – siehe
  „Zurückgestellt“ unten
- [ ] Tests (`tests/test_db_panel.py`): Verbindungstest gegen
  `:memory:`-SQLite, Tabellenbaum korrekt befüllt, SQL-Abfrage zeigt
  Ergebnis, CSV-Import erzeugt die erwartete Tabelle, CSV-/SQL-Export
  erzeugt gültige Dateien

### 9. Abnahme — noch offen

- [ ] Kontoverwaltung (`beispielprojekte/Kontoverwaltung/`, aufbauend
  auf `referenz/lazarus/n_konto`, aber mit echter SQLite-Persistenz
  statt reiner In-Memory-Klasse wie im Original – die Erweiterung um
  Datenbankanbindung ist der Zweck dieser Abnahme): `TKonto`-Logik
  (Ein-/Auszahlen, Überweisen) plus `DBGrid`/`DBNavigator`/`DBEdit` über
  eine `SQLite3Connection`
- [ ] CSV-Auswertung (`beispielprojekte/CsvAuswertung/`): Beispiel-CSV
  einlesen, `pandas.groupby`, Ergebnis in `StringGrid` (`load_dataframe`)
  und `Chart` (`add_bar_series`) anzeigen
- [ ] Würfelspiel-Highscore als HTML im Browser: bestehendes
  `beispielprojekte/Wuerfelspiel/` um einen „Highscore als HTML
  exportieren“-Knopf erweitert, der `open_url` auf die erzeugte Datei
  aufruft
- [ ] Alle drei mit eigenen Abnahmetests headless nachgewiesen (wie
  `tests/test_beispiel_ampel.py`), MariaDB-Teil siehe „Zurückgestellt“

## Zurückgestellt (nicht blockierend)

- [ ] echter Verbindungstest von `MySQLConnection` gegen eine laufende
  MariaDB-Instanz (Homeserver-Docker-Container, siehe „Stolperstein
  MariaDB“ oben) – Kontoverwaltungs-Abnahme läuft bis dahin nur gegen
  SQLite
- [ ] Zugangsdaten für Datenbankverbindungen dauerhaft verschlüsselt im
  Windows Credential Store ablegen (Abschnitt 10.2); MVP hält sie nur
  pro Sitzung im Speicher
- [ ] Debugger-Integration „Als Tabelle anzeigen“ für DataFrames/Listen/
  dicts im Variablen-Panel (Abschnitt 11.6) – gehört inhaltlich zu M4,
  wird hier nicht mit erledigt, um M5 nicht zu vermischen
- [ ] `matplotlib`-Diagramme direkt aus Konsolenprogrammen per
  `plt.show()` in eigenem Fenster (Abschnitt 11.6) – funktioniert bereits
  „umsonst“ über `matplotlib` selbst, sobald es als Abhängigkeit
  vorhanden ist, braucht aber noch einen eigenen Abnahmetest
- [ ] Drag & Drop einer Bilddatei aus Explorer/Windows-Explorer ins
  Formular (Abschnitt 11.4) – gehört eigentlich zum Designer (M3),
  hier nur der Bildbetrachter selbst
- [ ] Designzeit-Aktivierung nicht-visueller SQLdb-Komponenten im
  Designer/Objektinspektor (`connected = True` zeigt echte Daten im
  `DBGrid` des Designers, Abschnitt 10.1) – braucht eine eigene
  Designer-Integration für nicht-visuelle Komponenten (Verbindungen,
  Queries), die bisher nicht existiert; Laufzeitverhalten ist über
  `tests/test_data_controls.py` vollständig geprüft, nur die
  Design-Vorschau in der IDE selbst fehlt noch
