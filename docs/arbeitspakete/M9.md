# Arbeitspaket M9 – Diagramm-Editor

Siehe konzept-natter.md, Abschnitt 13 (Diagramm-Editor), 14 (Design-
Prüfer, Layout-Hinweise gelten sinngemäß). Kleinteilig wie M1–M8.

## Abnahmekriterium (aus dem Konzept)

UML-Klassendiagramm `TAmpel`, Struktogramm `ampel_zeichnen` und
Entscheidungstabelle der Ampel von Hand erstellen und als PDF
exportieren.

## Reihenfolge (Nutzer-Feedback September 2026)

Erst die MVP-Dreiergruppe aus dem Abnahmekriterium fertig und
abgenommen (Klassendiagramm, Struktogramm, Entscheidungstabelle),
danach Use-Case-, Aktivitäts-, Zustands- und Sequenzdiagramm als eigene
Schritte (Abschnitt 13.4: „Später“). Innerhalb der Dreiergruppe zuerst
das Fenster-Grundgerüst und das `.pdiag`-Format (beide werden von allen
drei gebraucht), dann das Klassendiagramm komplett (Formen → Bearbeiten
→ Verbindungen → Beschriften → Eigenschaften-Panel → Stilvorlagen/
Layout-Hinweise → Export/Druck – dieselbe Reihenfolge wie beim Designer
in M3: Anzeige vor Bearbeiten vor Verbindungen), zuletzt Struktogramm
und Entscheidungstabelle (strukturell näher an einer Baumstruktur bzw.
`StringGrid` als an Formen+Verbindungen, deshalb eigene Schritte statt
Wiederverwendung der Klassendiagramm-Canvas).

Fenster-Architektur (Nutzer-Feedback bestätigt): echtes zweites
Top-Level-Fenster mit eigenem Taskleisten-Eintrag, unabhängig vom
Hauptfenster verschiebbar (Abschnitt 13.1) – kein Dock/Tab in der IDE.

Export-Umfang (Nutzer-Feedback bestätigt): PNG, SVG, PDF und Drucken
gehören von Anfang an zum ersten fertigen Diagrammtyp, nicht erst
später nachgezogen.

### 1. `.pdiag`-Datenmodell und Fenster-Grundgerüst — erledigt

- [x] `schemas/pdiag.schema.json` um die beiden fehlenden Strukturen
  erweitert: Blockbaum für Struktogramme (`root` + rekursiver
  `block`-`$def` mit `children`/`then`/`else`/`cases`) und
  Bedingungs-/Aktionszeilen mit Regel-Spalten für
  Entscheidungstabellen. Die typabhängige Pflicht (`root` nur bei
  Struktogrammen, `conditions`/`actions` nur bei
  Entscheidungstabellen) über `allOf`/`if`/`then` statt nur als
  Kommentar – ein Struktogramm ohne `root` wird dadurch wirklich
  abgelehnt (eigener Test)
- [x] `ide/diagramm/datei.py`: `Diagramm` (Pfad + rohes `dict`),
  `laden()`/`speichern()` mit Schema-Validierung, Aufbau wie
  `ide/project/projekt.py`; `ide/diagramm/neu.py` legt den kleinsten
  gültigen Inhalt je Typ an (`leeres_diagramm`/`diagramm_erzeugen`)
- [x] `ide/diagramm/fenster.py`: `DiagrammFenster` als eigenständiges
  `QMainWindow` **ohne** Elternfenster (nur so vergibt Windows einen
  eigenen Taskleisten-Eintrag), alle sechs Menüs aus Abschnitt 13.2
  angelegt – umgesetzte Einträge aktiv, noch nicht umgesetzte
  ausgegraut statt vorzutäuschen; Statusleiste mit Formenanzahl,
  Raster, Einrasten, Seitenformat und Stilvorlage; gleiches Theme wie
  die IDE (dieselben `QSettings`)
- [x] `.pdiag`-Dateien aus `diagramme/` erscheinen im Projekt-Explorer
  in einer eigenen Gruppe „Diagramme“ (`Projekt.diagramme()`),
  Doppelklick öffnet das eigene Fenster statt eines Editor-Tabs;
  erneutes Öffnen holt das vorhandene Fenster nach vorne
- [x] „Datei → Neues Diagramm …“ im Hauptfenster fragt Typ (nur die
  drei MVP-Typen) und Namen ab, legt `diagramme/<name>.pdiag` an und
  öffnet es; „Datei → Speichern“/„Speichern unter …“ im
  Diagramm-Fenster selbst
- [x] Tests (29): `tests/test_diagramm_datei.py` (13 – Round-trip je
  Typ, Schema-Ablehnungen, Namensrückfall, kein Überschreiben),
  `tests/test_diagramm_fenster.py` (8 – elternloses Top-Level-Fenster,
  Menüs vollständig, ausgegraute Einträge, Statusleiste, Speichern/
  Speichern unter), `tests/test_hauptfenster_diagramm.py` (8 –
  Explorer-Gruppe, Doppelklick öffnet Fenster statt Tab, zweites
  Öffnen holt dasselbe Fenster, „Neues Diagramm“ inkl. Fehlerfälle)
- **Per Screenshot geprüft:** Explorer zeigt die Gruppe „Diagramme“ mit
  drei angelegten Diagrammen, das Diagramm-Fenster zeigt alle sechs
  Menüs und die Statusleiste „0 Formen │ Raster 8 px │ Einrasten ein │
  A4 quer │ Stil: modern-light“

### 2. Klassendiagramm: Formen anzeigen und platzieren — Formen erledigt, Ansicht offen

- [x] Formen-Palette (`ide/diagramm/palette.py`, links im Fenster):
  Klasse, abstrakte Klasse, Interface, Notiz, Paket mit Suchfeld,
  Gruppen je Diagrammtyp und Tooltip mit Kurzbeschreibung
  (Abschnitt 13.2); gleiches Interaktionsmuster wie die
  Komponentenpalette (Klick macht scharf, Klick auf die Fläche
  platziert dort, danach zurück zum Auswahlwerkzeug)
- [x] Entscheidung getroffen: Die Zeichenfläche (`ide/diagramm/canvas.py`)
  malt **alle** Formen selbst per `QPainter` in einem einzigen Widget,
  statt ein Widget je Form wie der Formular-Designer – Diagrammformen
  sind keine bedienbaren Steuerelemente, es werden schnell viele, und
  Verbindungen (Schritt 4) lassen sich ohnehin nur frei zeichnen. Das
  Zeichnen steht getrennt in `ide/diagramm/zeichnen.py`, damit der
  Export (Schritt 8) exakt dieselbe Darstellung malt
- [x] Normgerechte Darstellung (Abschnitt 13.6): eckige Klassen mit
  getrennten Bereichen für Name/Attribute/Methoden, abstrakte Klasse
  kursiv mit `{abstract}`, Interface mit `«interface»`, Notiz mit
  umgeknickter Ecke, Paket mit Reiter; Segoe UI für Namen, Consolas
  für Attribute/Methoden, 1,5-px-Linien mit Kantenglättung
- [x] Drei Stilvorlagen als Datenbasis angelegt (`ide/diagramm/stil.py`:
  Modern hell/dunkel, Schwarz-Weiß) – die Umschaltung in der
  Oberfläche folgt in Schritt 7
- [x] Punktraster, Einrasten am 8-px-Raster beim Platzieren,
  automatische Mindesthöhe, damit kein Text abgeschnitten wird
- [x] Tests (33 neu): `tests/test_diagramm_canvas.py` (14),
  `tests/test_diagramm_palette.py` (7), `tests/test_diagramm_fenster.py`
  (+5 für Palette/Statusleiste/Änderungsmarkierung)
- **Zwei echte Fehler beim Screenshot-Durchgang gefunden und behoben:**
  Attribut- und Methodenbereich wurden für die Mindesthöhe zusammen
  statt getrennt gerechnet – ein Interface mit zwei Methoden und ohne
  Attribute schnitt die letzte Methode ab; und die Mindesthöhe wurde
  auf die *nächste* Rasterlinie gerundet statt aufgerundet, konnte
  also knapp unter den nötigen Wert fallen
- [ ] Lineale, Minimap, Zoom (Strg+Mausrad, Strg+0, Strg+1),
  Verschieben der Ansicht (Leertaste+Ziehen) – eigener Teilschritt,
  siehe Schritt 2b

### 2b. Ansicht: Zoom, Verschieben, Lineale, Minimap

- [x] **Rollen (Scrollen) waagerecht und senkrecht** – vom Nutzer
  gemeldet. Die Zeichenflächen steckten in keinem Rollbereich und
  hatten eine feste Mindestgröße von 640×480; alles außerhalb des
  Fensters war damit nicht erreichbar, obwohl allein ein A4-Blatt quer
  schon 1123 px breit ist. Jede Fläche rechnet jetzt ihre Inhaltsgröße
  aus (Blatt bzw. Blockbaum bzw. Tabelle, plus Rand) und steckt in
  einer `QScrollArea`. Derselbe Fehler steckte im **Formular-Designer**:
  ein Formular, das größer war als der Tab-Bereich, wurde abgeschnitten
  – dort ohne `widgetResizable`, weil die Größe eines Formulars eine
  vom Nutzer gesetzte Eigenschaft ist und nicht von der Fenstergröße
  abhängen darf
- [x] Zoom im Klassendiagramm: Strg+Mausrad, Strg++/Strg+-, Strg+0
  („Alles anzeigen“ – rechnet die Stufe aus dem Sichtfenster),
  Strg+1 („100 %“), begrenzt auf 25 %–400 %. Die Zoomstufe steht in
  der Statusleiste. Nur das **Zeichnen** skaliert; jede Trefferprüfung
  rechnet weiter in Diagrammkoordinaten, sonst müsste jede einzelne
  Funktion den Zoom kennen. Der Texteditor in der Form skaliert
  Feldgeometrie **und** Schrift mit, sonst läge er bei 200 % neben
  seiner Form
- [x] Ansicht verschieben mit Leertaste+Ziehen und mit der mittleren
  Maustaste; das Verschieben geht dem Auswählen vor. Während des
  Beschriftens bleibt die Leertaste ein Leerzeichen
- [x] Zoom auch für Struktogramm und Entscheidungstabelle. Die
  Rechnung steckt jetzt in einer gemeinsamen Mischklasse
  `ide/diagramm/zoom.py`, die alle drei Flächen benutzen – sie
  mitzuschreiben wäre dreimal dieselbe Arbeit gewesen und hätte dreimal
  auseinanderlaufen können. Jede Fläche bringt nur noch
  `_inhalt_in_diagrammkoordinaten()` und ihr eigenes Signal
  `zoom_geaendert` mit; das Signal muss in der Klasse selbst stehen,
  weil PySide6 es in einer reinen Mischklasse nicht anmeldet
- [ ] Lineale (der Seitenbereich selbst ist mit Schritt 7 erledigt)
- [ ] Minimap
- [ ] Menü „Ansicht“ aktivieren (Zoom, Raster, Lineale, Minimap,
  Seitenränder) – bisher ausgegraut
- [ ] Tests: Zoomstufen, Raster/Lineale/Minimap an/aus, Seitenbereich

### 3. Klassendiagramm: Verschieben, Größe, Löschen, Duplizieren, Undo — erledigt

- [x] Einzelauswahl, acht Größenanfasser mit passendem Mauszeiger,
  Ziehen zum Verschieben und zum Größenändern
- [x] Einrasten am 8-px-Raster **und** an Kanten/Mitten anderer Formen
  mit gestrichelten Hilfslinien als Rückmeldung (Abschnitt 13.3 – über
  das reine Rasterschritt-Einrasten des Formular-Designers hinaus,
  dort gibt es keine Formen-zu-Formen-Hilfslinien)
- [x] Tastatur wie im Designer: Pfeil = Rasterschritt, Alt+Pfeil =
  1 px, Umschalt+Pfeil = Größe, Entf löscht, Strg+D dupliziert,
  Strg+Z/Strg+Umschalt+Z bzw. Strg+Y, Escape bricht das Platzieren ab
- [x] Mindestgröße wird beim Ziehen und per Tastatur eingehalten, damit
  kein Text abgeschnitten wird (Abschnitt 13.6)
- [x] Kommando-Muster für Rückgängig/Wiederholen, unbegrenzt innerhalb
  der Sitzung (`ide/diagramm/kommandos.py`); ein Ziehvorgang ist genau
  **ein** Undo-Schritt, obwohl die Live-Vorschau die Form laufend
  verändert. Der Stapel selbst ist dafür nach `ide/kommando.py`
  gewandert, weil ihn jetzt beide Editoren nutzen – der Diagramm-Editor
  soll nicht vom Formular-Designer abhängen
- [x] Menü „Bearbeiten“ (Rückgängig/Wiederholen/Duplizieren/Löschen)
  mit denselben Tastenkürzeln aktiviert
- [x] Tests (24 neu): jede Operation einzeln, Undo/Redo-Ketten, Ziehen
  als ein Schritt, Einrasten an fremden Kanten samt Hilfslinien,
  gelöschte Form kommt an ihrer alten Stelle der Zeichenreihenfolge
  zurück
- **Im Menü-Test gefunden:** nach „Wiederholen“ war nichts ausgewählt,
  ein direkt folgendes „Löschen“ lief deshalb ins Leere – eine wieder
  eingefügte Form wird jetzt auch wieder ausgewählt, wie beim ersten
  Platzieren

### 3b. Mehrfachauswahl und Anordnen — erledigt

- [x] Mehrfachauswahl: Rahmen aufziehen, Strg+Klick zum Dazunehmen und
  Wiederherausnehmen, Strg+A für „Alles auswählen“. Die Auswahl ist
  eine **Liste**; die zuletzt angeklickte Form ist die **führende** und
  als einzige mit Anfassern gezeichnet, alle übrigen gestrichelt. Daran
  richtet sich „Ausrichten“ aus – sonst müsste man raten, welche Form
  stehen bleibt (gleiches Verhalten wie in Lazarus und in Dia)
- [x] Der Rahmen nimmt nur, was **vollständig** in ihm liegt. Bloßes
  Berühren würde beim Aufziehen über ein dicht gestelltes Diagramm
  ständig Nachbarn mitnehmen
- [x] Ziehen und Pfeiltasten bewegen die ganze Auswahl, als **ein**
  Undo-Schritt. Eingerastet wird an der führenden Form, alle anderen
  folgen um denselben Betrag – würde jede Form einzeln einrasten,
  zerfiele eine sauber angeordnete Gruppe beim ersten Ziehen
- [x] Menü „Anordnen“ mit Untermenüs: Ausrichten (sechs Richtungen),
  Verteilen (waagerecht/senkrecht), Gleiche Größe (Breite/Höhe/beides).
  Verteilen ergibt gleiche **Lücken**, nicht gleiche Mittenabstände –
  bei unterschiedlich breiten Klassen sieht nur das gleichmäßig aus;
  die äußeren beiden Formen bleiben stehen
- [x] In den Vordergrund/Hintergrund über ein eigenes
  `ReihenfolgeKommando`, das die ganze Liste merkt statt einzelner
  Stellen
- [x] Gruppieren (Strg+G) und Aufheben (Strg+Umschalt+G). Eine Gruppe
  ist kein eigenes Element, sondern nur eine gemeinsame Kennung
  (`group`) an den Formen – so bleibt die Datei ohne Kenntnis von
  Gruppen lesbar
- [x] Kopieren/Ausschneiden/Einfügen als **JSON-Text** in der
  Zwischenablage. Text statt eigener MIME-Daten, weil
  `QClipboard.setMimeData()` PySide6 beim Beenden abstürzen lässt (in
  M9 reproduziert); nebenbei kann man den Text in einem Editor
  nachlesen. Verbindungen kommen mit, wenn beide Enden mitkopiert
  werden; fremder Text wird stillschweigend übergangen
- [x] Tests (53 neu): `tests/test_diagramm_anordnen.py`
- **In der Sichtprüfung gefunden und behoben:** nach „Ausrichten“ an
  einer weit rechts liegenden Klasse standen alle Formen außerhalb des
  sichtbaren Ausschnitts – die Fläche sah leer aus, als wären sie
  verschwunden. Die Ansicht rollt jetzt so weit nach, dass die Auswahl
  zu sehen ist, und auch nur so weit
- **Ebenfalls im Bild gefunden:** das Eigenschaften-Feld zeigte bei drei
  ausgewählten Formen kommentarlos die Werte einer einzigen. Es nennt
  jetzt die Zahl der mitausgewählten, weil eine Eingabe dort weiterhin
  nur die führende Form ändert

### 4. Klassendiagramm: Verbindungen — erledigt (Knickpunkte offen)

- [x] Alle sieben Verbindungsarten aus Abschnitt 13.4: Assoziation,
  gerichtete Assoziation, Aggregation ◇, Komposition ◆, Vererbung ▷,
  Abhängigkeit, Realisierung – je eigene Notation (leere/gefüllte
  Raute, offener Pfeil, leeres Dreieck, gestrichelte Linie) nach
  Abschnitt 13.6
- [x] Verbindung anlegen mit zwei Klicks (erst Quelle, dann Ziel),
  passend zum Platzieren von Formen; Klick ins Leere oder Escape
  bricht ab, eine Form mit sich selbst zu verbinden wird abgelehnt
- [x] Enden docken an: die Linie wird beim Zeichnen aus den beiden
  Formen berechnet und endet am Formrand statt in der Mitte, folgt
  also automatisch beim Verschieben
- [x] Verbindung anklickbar (Toleranzabstand zur Linie), auswählbar und
  löschbar; das Löschen einer Form nimmt ihre Verbindungen als **ein**
  Undo-Schritt mit, damit keine Verweise ins Leere zurückbleiben
- [x] Multiplizitäten/Rollen an den Enden
- [x] Tests (25 neu): `tests/test_diagramm_verbindungen.py`, u. a.
  jede Verbindungsart zeichnet sichtbar, Linie endet am Formrand,
  Verbindung folgt beim Verschieben, Speichern/Laden
- **Im Screenshot gefunden und behoben:** die Beschriftung lag halb in
  der Zielform und wurde von ihr überdeckt („0..*“ erschien als
  „0..“), an Aggregation/Komposition saß sie unter der Raute. Sie wird
  jetzt nach den Formen gezeichnet und entlang der Linie von der Form
  weg versetzt – hinter einer Raute/Pfeilspitze weiter
### 4b. Knickpunkte und Beschriftungen — erledigt

- [x] Doppelklick auf die Linie setzt einen Knickpunkt, Doppelklick auf
  einen vorhandenen nimmt ihn wieder weg. Der neue Punkt landet in dem
  **Linienstück, auf das geklickt wurde** (`segment_bei`) – ihn immer
  ans Ende zu hängen wäre bei einer schon geknickten Linie ein Sprung
  quer durchs Diagramm
- [x] Knickpunkte lassen sich ziehen, rasten am 8-px-Raster ein und
  sind ein einziger Undo-Schritt, obwohl die Live-Vorschau die Linie
  laufend verändert
- [x] Gezeichnet werden sie als kleine **Quadrate** und nur an der
  ausgewählten Verbindung: Kreise würde man mit den Anfassern einer
  Form verwechseln, und an allen Verbindungen gleichzeitig wäre das
  Diagramm mit Markierungen übersät
- [x] Beschriftungen frei verschiebbar (`label_offsets` je Ende).
  Zeichnen, Anklicken und Verschieben rechnen über **dieselbe**
  Funktion `beschriftungs_rechtecke()` – lägen die Zahlen doppelt vor,
  würde die Beschriftung irgendwann woanders gezeichnet als angeklickt
- [x] Tests (17 neu): `tests/test_diagramm_knickpunkte.py`

### 5. Klassendiagramm: Beschriften — erledigt, aber durch Schritt 12 abgelöst

> **Überholt (Nutzer-Entscheidung September 2026).** Die
> Direktbearbeitung auf der Zeichenfläche wird durch den
> Eigenschaften-Dialog aus **Schritt 12** ersetzt. Der hier
> beschriebene Stand bleibt als Verlauf stehen; was davon bestehen
> bleibt und was wegfällt, steht in Schritt 12.

- [x] Doppelklick (oder F2) legt Eingabefelder genau über die Bereiche
  der Form: Name oben, darunter Attribute und Methoden; Tab springt
  zum nächsten Feld, Escape bricht ab, Strg+Eingabe übernimmt
  (`ide/diagramm/textbearbeitung.py`). Die Bereichsgeometrie kommt aus
  `zeichnen.klassen_bereiche()`, die auch das Zeichnen benutzt – die
  Felder liegen dadurch exakt dort, wo der Text steht
- [x] Kurzeingabe: eine Zeile pro Attribut/Methode, Enter fügt eine
  Zeile hinzu, Strg+Pfeil sortiert um (Abschnitt 13.4); leere Zeilen
  fallen beim Übernehmen weg. Kein Prüfen auf syntaktische
  Richtigkeit, wie im Konzept festgehalten
- [x] Übernehmen ist ein Undo-Schritt und passt die Höhe gleich mit an,
  damit neue Zeilen nicht abgeschnitten werden
- [x] Notiz und Paket bekommen nur ein Namensfeld
- [x] Tests (15 neu): `tests/test_diagramm_beschriften.py`
- **Echter Absturz gefunden und behoben:** beim Abräumen eines Editors
  schickte Qt den sterbenden Feldern noch `FocusOut`, was erneut
  „übernehmen → abräumen“ anstieß und auf bereits gelöschte Widgets
  zugriff. Der Testlauf stürzte dadurch in einem *anderen* Test ab
  (`test_dialogs.py`, Windows: access violation). Der Editor wird jetzt
  vor dem Abräumen stillgelegt (Ereignisfilter abgemeldet, Filter
  wirkungslos); ein eigener Test deckt genau diesen Fall ab

### 6. Eigenschaften-Panel — erledigt

- [x] `ide/diagramm/eigenschaften.py`, rechts im Fenster
  (Abschnitt 13.2): Position/Größe, Füllung, Linie und Schriftgröße
  der ausgewählten Form bzw. die Beschriftungen einer ausgewählten
  Verbindung – strukturell wie `EigenschaftenTabelle` aus M3, aber
  eigenständig (Diagrammformen sind schlichte `dict`s, keine
  `pcl.Control`-Komponenten mit `Prop`-Deskriptoren). Es werden immer
  nur die Zeilen gezeigt, die zur Auswahl passen; ohne Auswahl steht
  nur „Nichts ausgewählt“ da
- [x] Jede Änderung im Panel läuft über denselben Kommando-Stapel wie
  eine Änderung mit der Maus, ist also genauso rückgängig machbar
- [x] Füllung/Linie sind **Übersteuerungen** der Stilvorlage: solange
  nichts eingestellt ist, zeigt der Farbknopf „(Stilvorlage)“ und die
  Form folgt weiter der in Schritt 7 umschaltbaren Vorlage
  (`shape.fill`/`line`/`font_size` neu im Schema, ausgewertet von
  `zeichnen.fuellfarbe()`/`randfarbe()`/`schriftgroesse()`)
- [x] „Format → Stil übertragen“ (Strg+Umschalt+V) in zwei Schritten:
  erster Aufruf merkt sich die ausgewählte Form als Vorlage, der
  zweite überträgt Füllung/Linie/Schrift auf die dann ausgewählte Form
- [x] Tests (10 neu): `tests/test_diagramm_eigenschaften.py`
- **Per Screenshot geprüft:** Panel rechts mit Links/Oben/Breite/Höhe,
  zwei Farbknöpfen und Schriftgröße bei ausgewählter Form; bei
  ausgewählter Verbindung stattdessen „Ende Quelle“/„Ende Ziel“ mit
  den Multiplizitäten; „Stil übertragen“ färbt die zweite Klasse
  sichtbar wie die erste

### 7. Stilvorlagen und Layout-Hinweise — erledigt

- [x] „Format → Stilvorlage“ als Untermenü mit den drei Vorlagen
  (Modern hell/Modern dunkel/Schwarz-Weiß, Abschnitt 13.6), für das
  ganze Diagramm umschaltbar und **unabhängig vom IDE-Theme**: ein im
  dunklen Theme gezeichnetes Diagramm lässt sich trotzdem als
  Schwarz-Weiß-Abgabe drucken. Umschalten ist rückgängig machbar und
  wird in der `.pdiag` gespeichert
- [x] `ide/diagramm/seite.py`: Seitenformat (A4/A3/A5, hoch/quer) in
  Pixel bei 96 dpi, mit Satzspiegel (Druckerrand). Die Zeichenfläche
  zeichnet Blattkante und Satzspiegel – ohne sie wäre der Hinweis
  „liegt außerhalb des Seitenbereichs“ nicht nachvollziehbar (damit ist
  der Seitenbereich aus Schritt 2b vorgezogen, dort bleiben Zoom,
  Verschieben, Lineale und Minimap)
- [x] `ide/diagramm/hinweise.py` mit vier Regeln (überlappende Formen,
  abgeschnittener Text, lose Verbindungsenden, Formen außerhalb des
  Seitenbereichs). Gleiches Versprechen wie der Design-Prüfer aus M7:
  nur Darstellung, nie Inhalt, blockiert nichts, abschaltbar über
  „Ansicht → Layout-Hinweise“. Betroffene Formen bekommen einen
  gestrichelten Warnrahmen, die Anzahl steht in der Statusleiste, die
  Meldungen selbst in deren Tooltip (eine betroffene Form kann
  außerhalb des sichtbaren Ausschnitts liegen)
- [x] „Ansicht → Raster“ und „Ansicht → Seitenränder“ als Ein/Aus-
  Schalter aktiviert
- [x] Tests (36 neu): `tests/test_diagramm_hinweise.py` (22 – je Regel
  ein auslösendes und ein unauffälliges Diagramm, Seitenmaße),
  `tests/test_diagramm_stilvorlagen.py` (14 – jede Vorlage wird
  wirklich am gemalten Bild geprüft, nicht nur am Datensatz)
- **Zwei echte Fehler beim Screenshot-Durchgang gefunden und behoben:**
  Notizen brechen ihren Text um, wurden aber wie Klassen auf
  Zeilenbreite geprüft und dadurch reihenweise fälschlich als „zu
  schmal“ gemeldet (jetzt zählt bei ihnen nur die umbrochene Höhe, die
  auch automatisch nachgezogen wird); und bei einer Überlappung war nur
  die erste der beiden Formen markiert – man sah also nicht, womit sie
  sich überlappt. Ein Hinweis nennt jetzt **alle** betroffenen Elemente

### 8. Export und Drucken — erledigt

- [x] `ide/diagramm/export.py` malt mit **demselben** Code wie die
  Zeichenfläche (`zeichnen.py`) und lässt nur weg, was zur Bedienung
  gehört: Raster, Auswahlrahmen, Hilfslinien, Warnrahmen der
  Layout-Hinweise. Maßgeblich ist immer die Stilvorlage des Diagramms,
  nie das Theme der IDE
- [x] PNG-Export mit wählbarer Auflösung (1×/2×/4×) und wahlweise
  durchsichtigem Hintergrund; die Rückfrage (`exportdialog.py`)
  erscheint nur bei PNG, weil SVG und PDF auflösungsfrei sind
- [x] SVG-Export (`QSvgGenerator`) – Text bleibt Text, nicht Pixel
- [x] PDF-Export (`QPdfWriter`) seitengetreu im Format der `.pdiag`,
  Auflösung auf 96 dpi gesetzt, damit eine PDF-Einheit genau einem
  Pixel der Zeichenfläche entspricht
- [x] „Bearbeiten → Als Bild kopieren“ legt das Diagramm in die
  Zwischenablage (Einfügen in Word o. Ä.) – mit **vierfacher**
  Auflösung (384 dpi) und eingetragener Auflösungsangabe, damit Word
  es scharf **und** in der richtigen Größe einfügt. Vom Nutzer
  gemeldet: vorher war es einfach aufgelöst und sichtbar unscharf.
  Bewusst ohne zusätzliches SVG im selben Datenpaket:
  `QClipboard.setMimeData()` lässt PySide6 beim Beenden mit einem
  Speicherzugriffsfehler abstürzen (hier reproduziert, auch mit
  gehaltener Referenz) – für Vektoren gibt es den SVG-Export
- [x] Drucken mit Seitenvorschau; „Anpassen an Seite“ verkleinert nur
  und vergrößert nie, damit ein kleines Diagramm nicht auf
  Plakatgröße aufgeblasen wird. Vorschau und echter Druck benutzen
  denselben Rückruf, können also nicht auseinanderlaufen
- [x] PNG/SVG beschneiden auf den Inhaltsbereich (plus Rand), PDF
  behält das volle Blatt – ein Diagramm mit drei Klassen soll kein
  Bild mit 80 % weißer Fläche ergeben
- [x] Tests (25 neu): `tests/test_diagramm_export.py`. Geprüft wird
  nicht nur, dass eine Datei entsteht, sondern dass sie wieder
  einlesbar ist: PNG über `QImage` samt Pixelfarben, SVG über einen
  XML-Parser, PDF über `QPdfDocument` mit Seitenzahl und Seitengröße
- **Echtes Problem gefunden:** der *erste* `QPrinter` eines Prozesses
  lässt Windows alle Drucker samt Treibern durchsuchen – auf diesem
  Rechner gemessene **48 Sekunden**, in denen die Oberfläche gestanden
  hätte. Der Drucker wird jetzt gemerkt (jeder weitere Aufruf kommt
  sofort) und während der Suche erscheinen Sanduhr und die Meldung
  „Drucker werden gesucht …“, damit niemand denkt, Natter sei
  abgestürzt. Die drei Tests, die das Drucksystem wirklich anfassen,
  tragen die Markierung `drucker` und lassen sich beim Entwickeln mit
  `-m "not drucker"` überspringen
- **Sichtnachweis:** die exportierte PDF wurde mit `QPdfDocument`
  zurückgelesen, als Bild gerendert und angesehen – A4 quer, alle
  Formen, Verbindungen und Multiplizitäten an der richtigen Stelle,
  Schwarz-Weiß-Vorlage ebenso

### 9. Struktogramm-Editor — erledigt (Verschieben/Kopieren offen)

- [x] Eigenes Datenmodell: Blockbaum statt Formen/Koordinaten
  (`ide/diagramm/bloecke.py`). Die Geometrie wird bei jedem Zeichnen
  aus dem Baum gerechnet, statt gespeichert zu werden – dadurch kann
  der Rahmen gar nicht erst aufbrechen, egal wie tief verschachtelt
  wird. Die Baumoperationen brauchen weder Qt noch ein Fenster und
  sind einzeln testbar
- [x] Alle acht Blocktypen aus Abschnitt 13.5: Anweisung, Verzweigung
  (ja/nein), Mehrfachauswahl, Zählschleife, kopf- und fußgesteuerte
  Schleife, Unterprogrammaufruf, Aussprung – Layout und Notation in
  `ide/diagramm/struktogramm.py`
- [x] Bedient wird über **Einfügestellen**: wer einen Block aus der
  Palette gewählt hat, sieht die Lücke hervorgehoben, in die er käme
  (zwischen zwei Blöcken, in einen leeren Zweig, in einen
  Schleifenkörper). Dadurch kann nie ein Block „daneben“ landen.
  Gleiches Muster wie die Formen-Palette: Klick macht scharf, Klick
  auf die Fläche setzt, Escape bricht ab
- [x] Nachbarspalten werden automatisch auf dieselbe Höhe gezogen,
  damit unten kein Loch im Rahmen bleibt; Blöcke wachsen mit ihrem
  Text mit
- [x] Mehrfachauswahl: Fälle hinzufügen/entfernen, die letzte Spalte
  bleibt stehen (eine Auswahl ohne Fall wäre kein sinnvoller Block)
- [x] Löschen nimmt den Inhalt mit und setzt ihn bei „Rückgängig“ an
  genau dieselbe Stelle zurück; Enter legt eine Anweisung darunter an,
  Entf löscht, F2/Doppelklick beschriftet – alles über denselben
  Kommando-Stapel wie das Klassendiagramm
- [x] Menüeinträge, die es für Struktogramme nicht gibt (Duplizieren,
  Stil übertragen, Raster, Layout-Hinweise), sind ausgegraut statt
  vorgetäuscht; die Statusleiste zählt Blöcke statt Formen
- [x] Export/Drucken über dieselbe Infrastruktur wie Schritt 8
- [x] Tests (56 neu): `tests/test_diagramm_bloecke.py` (26 – Baum und
  Layout ohne Fenster), `tests/test_diagramm_struktogramm.py` (30 –
  Einfügestellen je Blocktyp, Auswahl, Undo/Redo, Palette, Fenster)
- **Zwei Fehler im Screenshot-Durchgang gefunden:** das Paletten-Dock
  hieß noch „Formen“, obwohl ein Struktogramm keine Formen kennt; und
  im PDF klebte das Struktogramm am Blattrand, weil sein Layout bei
  (0, 0) beginnt – Formen-Diagramme tragen dagegen schon ihre eigenen
  Seitenkoordinaten. Es bekommt jetzt den Druckrand als Versatz
- [x] Blöcke mit der Maus **verschieben** und mit Strg **kopieren**.
  Gezogen wird erst ab sechs Pixeln – sonst würde jeder Klick, bei dem
  die Hand zittert, schon etwas umhängen. Herausnehmen und Einsetzen
  zusammen sind **ein** Undo-Schritt
- [x] Loslassen geht auch **auf** einem Block, nicht nur in der Fuge:
  obere Hälfte heißt davor, untere dahinter. `stelle_bei()` trifft nur
  die schmalen Lücken – beim Einfügen aus der Palette zielt man genau
  dorthin, beim Ziehen aber nicht
- [x] Abgelehnt werden: ein Block in sich selbst oder in einen eigenen
  Zweig (das ergäbe einen Kreis), und die Stelle, an der er ohnehin
  schon steht (das wäre ein Undo-Schritt, der nichts tut). Beim
  Umrechnen der Zielnummer zählt die Zielstelle den Block noch mit, der
  gerade herausgenommen wird – ohne diese Korrektur landete der erste
  Block beim Ziehen ans Ende eine Stelle zu weit vorn
- [x] Die Kopie bekommt durchweg neue Kennungen; zwei Blöcke mit
  derselben `id` würden die Auswahl durcheinanderbringen
- [x] **Kopfzeile mit dem Namen** des Struktogramms. Nassi und
  Shneiderman schreiben den Namen des Algorithmus über den Block – ohne
  ihn ist auf einem ausgedruckten Blatt nicht zu erkennen, zu welcher
  Methode das Struktogramm gehört. Der Versatz steckt im **Layout**,
  nicht im Zeichencode: dadurch stimmen Zeichnen und Trefferprüfung von
  allein überein
- [x] Tests (21 neu): `tests/test_diagramm_bloecke_ziehen.py`
- **Beim Bauen der Sichtprüfung selbst hineingelaufen:** ein
  Kinderlisten-Schlüssel, den der Blocktyp gar nicht kennt (`"otherwise"`
  statt `"else"`), legte über `setdefault` stillschweigend ein Feld an,
  das keine Darstellung hat. Der eingefügte Block war unsichtbar, stand
  aber in der Datei, und die Schema-Prüfung schlug beim nächsten Öffnen
  zu. `Einfuegestelle.liste()` lehnt unbekannte Schlüssel jetzt ab
- [ ] **Mehrere Struktogramme auf einer Seite.** Zurückgestellt: die
  `.pdiag` führt genau ein `root`, und mehrere würden durch
  Trefferprüfung, Quelltexterzeugung und Export durchschlagen. Für den
  Unterricht ist ein Struktogramm je Datei der Normalfall – wer zwei
  nebeneinander drucken will, legt zwei Dateien an

### 10. Entscheidungstabelle-Editor — erledigt

- [x] `ide/diagramm/tabelle.py` (Layout und Zeichnen) und
  `ide/diagramm/tabelle_canvas.py` (Bedienung). Gemalt wird mit
  `QPainter` statt mit einem `QTableWidget` – aus demselben Grund wie
  beim Struktogramm: Bildschirm und Export sollen denselben Code
  benutzen, damit die Abgabe genau so aussieht wie das, was auf dem
  Bildschirm stand
- [x] Zellen durchschalten per Klick oder Leertaste: Bedingungsteil
  `J` → `N` → `*` → leer, Aktionsteil `X` → leer. Ein Klick auf die
  Textspalte schaltet **nichts** – sonst verstellte das Anwählen einer
  Zeile aus Versehen einen Wert
- [x] Eigenes Menü „Tabelle“: Bedingung/Aktion hinzufügen, Zeile
  entfernen, Regel hinzufügen/entfernen, Regel nach links/rechts.
  Eine Regel-Spalte betrifft immer **alle** Zeilen beider Teile, sonst
  liefen Bedingungs- und Aktionsteil auseinander; die letzte Regel
  bleibt stehen
- [x] Alles über denselben Kommando-Stapel, also rückgängig machbar;
  Statusleiste zählt Zeilen und Regeln
- [x] Keine automatische Zusammenfassung oder Vollständigkeitsprüfung
  von Regeln (bewusst, wie im Konzept festgehalten)
- [x] Export/Drucken über dieselbe Infrastruktur wie Schritt 8
- [x] Tests (32 neu): `tests/test_diagramm_tabelle.py`
- **Echter Fehler im PDF gefunden:** die Kopfzeilen waren mit der
  Trennlinienfarbe hinterlegt. In der Schwarz-Weiß-Vorlage ist die
  schwarz – „Bedingungen“, „Aktionen“ und die Regelnummern
  verschwanden also ausgerechnet in der Vorlage, die für die Abgabe auf
  Papier gedacht ist. Die Stilvorlagen haben jetzt eine eigene
  Kopfzeilenfarbe; ein Test prüft am gemalten Bild, dass in der
  Kopfzeile noch Text zu sehen ist
- **Im Screenshot gefunden:** das Menü „Tabelle“ landete hinter
  „Hilfe“, weil es erst nachträglich angehängt wurde – es wird jetzt
  davor eingehängt

### 11. Abnahme der MVP-Dreiergruppe — bestanden

Alle drei Diagramme liegen als Beispiel im Projekt
(`beispielprojekte/Ampel/diagramme/`) und wurden ausschließlich über
die Editor-Methoden erzeugt, die auch Maus und Tastatur benutzen.

- [x] `tampel_klassen.pdiag`: Klasse `Ampel` mit ihren beiden privaten
  Attributen und allen sechs Methoden aus `beispielprojekte/Ampel/
  u_ampel.py`, dazu `Form1` mit den vier Ereignisprozeduren, verbunden
  über eine Aggregation `1 – 1`, und eine Notiz zur Zustandscodierung
- [x] `ampel_zeichnen.pdiag`: Struktogramm der Prozedur aus
  `beispielprojekte/Ampel/u_main.py` – Verzweigung „Ampel
  ausgeschaltet?“ mit Aussprung im Ja-Zweig, im Nein-Zweig die
  Mehrfachauswahl über `phase` mit den Fällen `1`, `3` und `2 oder 4`
- [x] `ampel_entscheidung.pdiag`: Entscheidungstabelle mit drei
  Bedingungen, vier Aktionen und vier Regeln
- [x] Alle drei als PDF exportiert, mit `QPdfDocument` **zurückgelesen**,
  als Bild gerendert und angesehen: je eine Seite im richtigen Format
  (Klassendiagramm und Tabelle A4 quer, Struktogramm A4 hoch), Inhalt
  vollständig und an der richtigen Stelle
- [x] Tests (15 neu): `tests/test_diagramm_abnahme.py` lädt die drei
  Dateien, exportiert sie nach `tmp_path` (im Beispielprojekt wird
  nichts verändert, siehe AGENTS.md), prüft, dass die Bilder nicht leer
  sind, dass die Methoden im Klassendiagramm zu `u_ampel.py` passen,
  dass die Entscheidungstabelle in jeder Regel-Spalte genau eine Aktion
  hat, und dass der Layout-Prüfer aus Schritt 7 nichts zu bemängeln hat
- **Beim ersten Durchgang hat der Layout-Prüfer sich selbst bewährt:**
  er meldete „Ampel ist zu schmal, der Text wird abgeschnitten“, und im
  PDF war die Methodenzeile tatsächlich abgeschnitten. Die Klassen
  wurden daraufhin verbreitert – genau der Ablauf, den die Hinweise
  auslösen sollen

### 12. UML-Klassen über einen Eigenschaften-Dialog bearbeiten

**Nutzer-Entscheidung September 2026, mit Bildschirmfotos belegt:** die
Inhalte einer UML-Klasse werden **nicht** direkt auf der Zeichenfläche
bearbeitet, sondern über einen Eigenschaften-Dialog mit Reitern – so
wie es das Programm Dia macht („Eigenschaften: UML – Class“). Wörtlich:
„so sollen die uml diagramme gebaut werden. es soll nicht in der
oberfläche direkt bearbeitbar sein.“

Damit ist die Formulierung im Konzept, Abschnitt 13.3 („bearbeitet
direkt in der Form“), überholt. Die Zeichenfläche bleibt zuständig für
**Anordnen** (platzieren, verschieben, Größe, verbinden), der Dialog
wird zuständig für **Inhalt**.

Warum das die bessere Lösung ist: ein Attribut ist kein Textschnipsel,
sondern hat Name, Typ, Vorgabewert und Sichtbarkeit; eine Operation
zusätzlich eine Parameterliste. Als freier Text ließ sich das weder
prüfen noch normgerecht darstellen – `+setzen(farbe: str)` war bisher
eine Zeichenkette, die zufällig wie UML aussah.

#### 12.1 Datenmodell umstellen

Attribute und Operationen werden aus freien Zeichenketten zu
strukturierten Einträgen. `schemas/pdiag.schema.json` wird entsprechend
erweitert (neue `$defs`), der bisherige `text`-Block mit
`attributes: [string]` und `methods: [string]` entfällt.

- [ ] `$defs/attribut`: `name`, `type`, `value`, `comment`,
  `visibility` (`public`/`private`/`protected`/`implementation`),
  `class_scope` (bool)
- [ ] `$defs/operation`: `name`, `type` (Rückgabetyp), `stereotype`,
  `comment`, `visibility`, `inheritance` (`abstract`/`virtual`/`leaf`),
  `class_scope` (bool), `query` (bool, „Anfrage“), `parameters`
- [ ] `$defs/parameter`: `name`, `type`, `default`, `comment`,
  `direction` (`undefined`/`in`/`out`/`inout`)
- [ ] `$defs/vorlagenparameter`: `name`, `type` (für Vorlageklassen)
- [ ] Klasse selbst zusätzlich: `stereotype`, `comment`, `abstract`,
  `template` (bool) und `template_parameters`
- [ ] Anzeigeschalter je Klasse, weil sie zum Diagramm gehören und
  nicht zur Stilvorlage: `attributes_visible`, `operations_visible`,
  `attributes_suppressed`, `operations_suppressed`,
  `comments_visible`, `wrap_operations`, `wrap_after_operations`
  (Vorgabe 40), `wrap_after_comments` (Vorgabe 17),
  `show_documentation`
- [ ] **Migration:** vorhandene `.pdiag` mit den alten Textlisten
  müssen weiter ladbar bleiben. Beim Laden werden Zeilen wie
  `-__zustand: int` bzw. `+get_zustand(): int` einmalig in die neue
  Struktur zerlegt (führendes `+`/`-`/`#`/`~` → Sichtbarkeit, Teil vor
  `:` → Name, danach → Typ, Klammern → Operation mit Parametern). Ein
  eigener Test deckt genau diese Umsetzung ab, und die drei
  Abnahmediagramme in `beispielprojekte/Ampel/diagramme/` werden
  danach einmal neu geschrieben

#### 12.2 Der Dialog

Ein modaler Dialog mit fünf Reitern, unten die drei Knöpfe
**Schließen**, **Anwenden**, **OK** (Anwenden übernimmt, ohne zu
schließen). Geöffnet wird er per Doppelklick auf die Form, per F2 und
über „Bearbeiten → Eigenschaften …“.

- [ ] **Reiter „Klasse“:** Klassenname, Stereotyp, Kommentar
  (mehrzeilig); Schalter „Abstrakt“, „Attribute sichtbar“,
  „Operationen sichtbar“, „Operationen umbrechen“, „Kommentare
  sichtbar“, „Dokumentationsauszeichnung anzeigen“; rechts daneben
  „Attribute unterdrücken“, „Operationen unterdrücken“ und zwei
  Zahlenfelder „Umbruch nach dieser Länge“ (für Operationen und für
  Kommentare)
- [ ] **Reiter „Attribute“:** Liste der Attribute mit den Knöpfen
  **Neu**, **Löschen**, **Hoch**, **Runter**; darunter die Gruppe
  „Attributdaten“ mit Name, Typ, Wert, Kommentar, Sichtbarkeit
  (Auswahlfeld) und „Klassen-Gültigkeitsbereich“. Die Felder sind
  ausgegraut, solange nichts ausgewählt ist
- [ ] **Reiter „Operationen“:** Liste wie bei den Attributen; Gruppe
  „Operationsdaten“ mit Name, Typ, Stereotyp, Sichtbarkeit, Typ der
  Vererbung, „Klassen-Gültigkeitsbereich“, „Anfrage“ und Kommentar;
  darunter eine **eigene Parameterliste** (Neu/Löschen/Hoch/Runter)
  mit der Gruppe „Parameterdaten“: Name, Typ, Vorgabewert, Richtung,
  Kommentar
- [ ] **Reiter „Vorlagen“:** Schalter „Vorlageklasse“, Liste der
  formellen Parameter mit Neu/Löschen/Hoch/Runter und der Gruppe
  „Formelle Parameterdaten“ mit Name und Typ
- [ ] **Reiter „Stil“:** Füllung, Linie und Schriftgröße – das, was
  heute im Eigenschaften-Bereich rechts steht (Schritt 6). Der Bereich
  rechts bleibt für Position und Größe zuständig, weil man die beim
  Anordnen laufend braucht
- [ ] Ein Dialogdurchgang ist **ein** Undo-Schritt, egal wie viele
  Felder geändert wurden. „Anwenden“ mehrfach ergibt entsprechend
  mehrere Schritte
- [ ] „Schließen“ verwirft, was seit dem letzten „Anwenden“ geändert
  wurde

#### 12.3 Darstellung anpassen

- [ ] `zeichnen.py` setzt die Zeilen aus den strukturierten Daten
  zusammen statt sie durchzureichen: Sichtbarkeitszeichen (`+`, `-`,
  `#`, `~`), `name: typ`, bei Operationen die Parameter in Klammern,
  Rückgabetyp nach dem Doppelpunkt
- [ ] Klassen-Gültigkeitsbereich wird unterstrichen dargestellt,
  abstrakte Operationen kursiv (Abschnitt 13.6)
- [ ] Die Anzeigeschalter wirken wirklich: unterdrückte Bereiche
  verschwinden ganz, unsichtbare bleiben als leerer Bereich stehen;
  „Operationen umbrechen“ bricht nach der eingestellten Länge um
- [ ] `mindesthoehe`/`mindestbreite` rechnen mit den neuen Zeilen

#### 12.4 Was wegfällt

- [ ] `ide/diagramm/textbearbeitung.py` (Direktbearbeitung) entfällt
  samt `tests/test_diagramm_beschriften.py`. Der darin festgehaltene
  Absturz (sterbende Eingabefelder lösten über `FocusOut` erneut
  „übernehmen“ aus) ist mit dem Wegfall gegenstandslos – die Ursache
  gehört trotzdem in die Begründung des Commits, damit sie nicht in
  einer späteren Direktbearbeitung wieder gebaut wird
- [ ] Der Zoom-Sonderfall „Editor skaliert Feldgeometrie und Schrift
  mit“ entfällt damit ebenfalls

#### 12.5 Tests

- [ ] Jeder Reiter einzeln: Eintrag anlegen, ausfüllen, umsortieren,
  löschen
- [ ] Ein Dialogdurchgang ist ein Undo-Schritt; „Schließen“ verwirft
- [ ] Alte `.pdiag` mit Textlisten wird korrekt in die neue Struktur
  umgesetzt (je ein Fall für Attribut, Operation mit Parametern,
  Sichtbarkeitszeichen, fehlender Typ)
- [ ] Gezeichnete Zeile stimmt mit dem erwarteten UML-Text überein
- [ ] Anzeigeschalter wirken (unterdrückt ≠ unsichtbar)
- [ ] Sichtprüfung: das `TAmpel`-Abnahmediagramm sieht nach der
  Umstellung genauso aus wie vorher

#### 12.6 Geklärt (Nutzer, September 2026)

- [x] **Struktogramm und Entscheidungstabelle bleiben in der Fläche
  bedienbar.** Wörtlich: „Struktogramm und Tabelle sollen in Fläche
  funktionieren.“ Dort ändert sich also nichts – Zelle durchschalten
  per Klick, Block beschriften per F2
- [x] **Notiz und Paket bleiben ebenfalls direkt bearbeitbar.** Beide
  haben nur ein einziges Textfeld; ein Dialog mit Reitern wäre dafür
  überzogen. Der Dialog gilt für **Klasse, abstrakte Klasse und
  Interface** – also genau für die Formen mit Attributen und
  Operationen

### 13. Klasse als Python-Quelltext ausgeben

**Nutzer-Wunsch September 2026:** aus einer im Diagramm modellierten
Klasse soll sich der passende Python-Quelltext erzeugen lassen – **in
eine eigene Datei oder in ein Fenster, je nach Auswahl** –, um ihn von
dort zu übernehmen.

Das ist der eigentliche Gewinn aus Schritt 12: erst weil Attribute und
Operationen strukturiert vorliegen (Name, Typ, Sichtbarkeit,
Parameter), lässt sich daraus überhaupt sinnvoller Code erzeugen. Aus
freiem Text ginge es nicht.

Dia kann das übrigens auch, über XSLT-Vorlagen
(`dia-uml2python.xsl`, dazu Java, C++, C, IDL). Dessen Ausgabe ist
allerdings alt: Sichtbarkeit als Kommentar (`#private:`), keine
Typangaben, `return None` als Rumpf. Natter erzeugt stattdessen
Quelltext im Stil der eigenen Beispielprojekte.

#### 13.1 Was erzeugt wird

Maßstab ist `beispielprojekte/Ampel/u_ampel.py` – so sieht in diesem
Projekt handgeschriebener Code aus, und so soll auch der erzeugte
aussehen:

```python
class Ampel:
    """Eine Ampel mit vier Zuständen."""

    def __init__(self, eingeschaltet: bool, zustand: int) -> None:
        self.__eingeschaltet = eingeschaltet
        self.__zustand = zustand

    def einschalten(self) -> None:
        ...
```

- [ ] Sichtbarkeit über die Namensschreibweise statt über Kommentare:
  `private` → `__name`, `protected` → `_name`, `public` → `name`
- [ ] Typangaben aus dem UML-Typ übernehmen (`zustand: int`,
  `-> bool`); fehlt der Typ, wird die Angabe weggelassen statt
  geraten
- [ ] `__init__` aus den Attributen erzeugen, mit den Vorgabewerten
  aus dem Feld „Wert“
- [ ] Kommentare aus dem Dialog werden zu Docstrings
- [ ] Operationsrümpfe bleiben leer (`...`), damit niemand denkt, hier
  stünde schon Logik. Abstrakte Operationen bekommen zusätzlich
  `raise NotImplementedError`
- [ ] Klassen-Gültigkeitsbereich → `@staticmethod` bzw.
  Klassenattribut; „Anfrage“ → `@property`, wenn die Operation keine
  Parameter hat
- [ ] Vererbung aus den Verbindungen: eine **Verallgemeinerung** wird
  zur Basisklasse (`class TAmpel(Fahrzeug):`), eine **Realisierung**
  ebenso. Aggregation und Komposition werden zu Attributen
- [ ] Vorlageklassen (Reiter „Vorlagen“) → `typing.Generic`
- [ ] Deutsche Umlaute in Namen bleiben erhalten – Python erlaubt sie,
  und in dieser Zielgruppe kommen sie vor

#### 13.2 Bedienung

- [ ] Neuer Menüeintrag „Klasse → Quelltext erzeugen …“ (bzw. im
  Kontextmenü der Form), aktiv nur bei Klassendiagrammen
- [ ] Ein kleiner Dialog fragt zweierlei:
  - **Ziel:** „In einem Fenster anzeigen“ oder „In eine Datei
    schreiben“
  - **Umfang:** „Nur die ausgewählte Klasse“ oder „Alle Klassen des
    Diagramms“
- [ ] Die getroffene Wahl wird als Vorgabe für das nächste Mal
  gemerkt (`QSettings`, wie die übrigen Einstellungen)

#### 13.3 Ausgabe in ein Fenster

- [ ] Ein eigenes Fenster mit dem erzeugten Quelltext, **nur
  lesbar** – es ist eine Vorlage zum Übernehmen, kein zweiter Editor
- [ ] Syntaxhervorhebung über den vorhandenen `QuelltextEditor`
  (`ide/shell/quelltexteditor.py`), damit es aussieht wie im Editor
- [ ] Knöpfe: **Kopieren** (alles in die Zwischenablage),
  **Speichern unter …** und **Schließen**
- [ ] Mehrere Klassen kommen nacheinander in dieselbe Ansicht, durch
  Leerzeilen getrennt

#### 13.4 Ausgabe in eine Datei

- [ ] Vorschlag: `units/<klassenname>.py` im aktuellen Projekt,
  kleingeschrieben, mit `u_`-Präfix passend zur Projektkonvention
- [ ] Eine **vorhandene Datei wird nie stillschweigend überschrieben**
  – es gibt eine Rückfrage mit den drei Möglichkeiten „Überschreiben“,
  „Anderen Namen wählen“ und „Abbrechen“. Wer eine Klasse zweimal
  erzeugt, soll nicht seine ausformulierten Methodenrümpfe verlieren
- [ ] Nach dem Schreiben erscheint die Datei im Projekt-Explorer und
  wird auf Wunsch gleich im Editor geöffnet
- [ ] Bei „Alle Klassen“ je Klasse eine eigene Datei

#### 13.5 Was bewusst nicht gemacht wird

- [ ] **Kein Rückweg** von Code zu Diagramm und kein Abgleich in
  beide Richtungen. Das wäre ein eigenes, großes Thema und geht in
  Schulprojekten regelmäßig schief, sobald jemand den erzeugten Code
  von Hand weiterschreibt
- [ ] **Kein Einfügen in bestehende Dateien.** Erzeugt wird immer eine
  vollständige, frische Datei; das Zusammenführen bleibt Handarbeit –
  dafür ist die Fensterausgabe zum Kopieren da

#### 13.6 Tests

- [ ] Je ein Test für Sichtbarkeit, Typangaben, Vorgabewerte,
  Docstrings, abstrakte Operationen, `@staticmethod`, `@property`
- [ ] Vererbung: Verallgemeinerung wird zur Basisklasse; zwei
  Basisklassen ergeben Mehrfachvererbung in der Reihenfolge der
  Verbindungen
- [ ] Der erzeugte Quelltext ist **gültiges Python**: `ast.parse()`
  über jedes Ergebnis, zusätzlich `ruff check` über eine erzeugte
  Beispieldatei
- [ ] Das `TAmpel`-Abnahmediagramm erzeugt eine Klasse, deren
  Methodennamen mit `beispielprojekte/Ampel/u_ampel.py` übereinstimmen
- [ ] Vorhandene Datei wird ohne Bestätigung nicht überschrieben
- [ ] Fensterausgabe ist nur lesbar und der Kopieren-Knopf legt den
  vollständigen Text in die Zwischenablage


### 14. Struktogramm als Quelltext ausgeben

**Nutzer-Wunsch September 2026:** „ich möchte auf jeden Fall Codeteile
als Script sehen können und auch das Gesamte als Code in dem Fenster
oder als extra Datei sehen können.“ Vorbild ist Structorizer, das
genau das kann.

Das ist das Gegenstück zu Schritt 13 (UML-Klasse → Quelltext), nur für
Struktogramme – und hier ist es noch näherliegend, weil ein
Struktogramm bereits ein Algorithmus ist. Ein Struktogramm zeichnen,
den Code daneben sehen und ihn übernehmen: genau der Weg, den der
Unterricht vorgibt.

#### 14.1 Zwei Umfänge

- [ ] **Ausschnitt** („Codeteile als Script“): ist ein Block
  ausgewählt, wird **nur dieser Block samt allem, was in ihm steckt**
  ausgegeben – eine Verzweigung also mit beiden Zweigen, eine Schleife
  mit ihrem Körper. Das Ergebnis ist ein Schnipsel zum Kopieren, ohne
  Funktionskopf, aber sinnvoll eingerückt
- [ ] **Ganzes Struktogramm:** ohne Auswahl (oder auf Wunsch) wird
  alles ausgegeben, eingepackt in eine Funktion mit dem Namen des
  Struktogramms (`name` aus der `.pdiag`, z. B. `ampel_zeichnen`)
- [ ] Dieselbe Zielauswahl wie in Schritt 13: **Fenster** (nur lesbar,
  mit Syntaxhervorhebung, Knöpfe Kopieren / Speichern unter /
  Schließen) oder **Datei**

#### 14.2 Übersetzung der Blöcke nach Python

| Block | Python |
|---|---|
| Anweisung | die Zeile selbst |
| Verzweigung | `if bedingung:` / `else:` |
| Mehrfachauswahl | `match`/`case`, bei einfachen Werten sonst `if`/`elif` |
| Zählschleife | `for i in range(...):` |
| Kopfgesteuerte Schleife | `while bedingung:` |
| Fußgesteuerte Schleife | `while True:` … `if bedingung: break` |
| Unterprogrammaufruf | der Aufruf selbst |
| Aussprung | `break`, `continue` oder `return`, je nach Text |

- [ ] Der Blocktext wird **nicht** übersetzt: was jemand
  `zustand := zustand + 1` nennt, bleibt so stehen. Struktogramme
  werden im Unterricht in Pseudocode beschriftet, und ein
  halbautomatischer Übersetzer würde mehr Verwirrung stiften als
  helfen. Stattdessen:
- [ ] Zeilen, die kein gültiges Python sind, werden **als Kommentar**
  ausgegeben (`# zustand := zustand + 1`) und oben im Ergebnis
  gesammelt aufgeführt („3 Zeilen konnten nicht übernommen werden“).
  So bleibt der erzeugte Code immer lauffähig, und es ist sofort zu
  sehen, was noch von Hand nachzuziehen ist
- [ ] Ein leerer Zweig bzw. Schleifenkörper wird zu `pass`

#### 14.3 Tests

- [ ] Je Blocktyp ein Test: erzeugte Zeilen stimmen mit dem erwarteten
  Python überein, Einrückung stimmt bei Verschachtelung
- [ ] Ausschnitt einer Verzweigung enthält beide Zweige, aber keinen
  Funktionskopf
- [ ] Das ganze `ampel_zeichnen`-Struktogramm ergibt gültiges Python
  (`ast.parse()`)
- [ ] Pseudocode-Zeilen landen als Kommentar, und ihre Zahl wird
  gemeldet
- [ ] Leerer Zweig ergibt `pass`

#### 14.4 Erkenntnisse aus Structorizer

Aus der lokalen Installation ausgelesen (`Structorizer.jar`,
Version 3.32-35, `generators.xml`/`parsers.xml`/`controllers.xml`):

**Code-Export in 16 Sprachen** – Pascal/Delphi, Oberon, Perl, ksh,
bash, C, C#, C++, Java, JavaScript, PHP, **Python**, BASIC, ARM-
Assembler, StrukTeX, LaTeX/Algorithm. Für Natter ist nur Python
relevant; die Breite zeigt aber, dass die Übersetzung sauber vom
Blockmodell getrennt gehört, damit später eine zweite Sprache möglich
bleibt, ohne alles umzubauen.

**Bild-Export:** PNG, PNG gekachelt, EMF, PDF, SVG – Natter hat PNG,
SVG und PDF bereits (Schritt 8). „Gekachelt“ ist einen Gedanken wert:
ein großes Struktogramm über mehrere Blätter verteilt ausgeben.

**Was Natter an Blocktypen fehlt** (Structorizer hat elf, Natter
acht):

**Nutzer-Entscheidung September 2026: alle drei werden ergänzt**, damit
Natter hier mit Structorizer gleichzieht. Sie gehören mit in Schritt 14,
weil die Codeerzeugung sie ohnehin abbilden muss:

- [ ] **Endlosschleife** (`forever`) → `while True:`
- [ ] **Parallelabschnitt** (`para`) → die Zweige nacheinander, mit
  einem Kommentar, dass sie eigentlich nebenläufig gedacht sind.
  Echte Nebenläufigkeit zu erzeugen wäre für die Zielgruppe irreführend
- [ ] **Try-Block** (`try`) → `try:` / `except:` / `finally:`
- [ ] Schema, Palette, Layout, Zeichnen und Tests je Blocktyp wie bei
  den vorhandenen acht

**Bewusst nicht übernommen:**

- **Code-Import** (Quelltext → Struktogramm; Structorizer kann Pascal,
  ANSI-C99, COBOL, Java, Processing). Dasselbe Argument wie in
  Schritt 13.5: ein Rückweg geht kaputt, sobald jemand den Code von
  Hand weiterschreibt
- **Eigener Debugger für Struktogramme** (Structorizer führt
  Struktogramme schrittweise aus und zeigt die Variablen). Reizvoll
  für den Unterricht – aber Natter hat mit M4 bereits einen echten
  Debugger für echten Python-Code. Zwei Debugger mit
  unterschiedlichem Verhalten wären für Lernende eher verwirrend. Wer
  den Ablauf sehen will, erzeugt den Code und setzt dort einen
  Haltepunkt
- **Turtleizer** (Turtle-Grafik aus dem Struktogramm heraus). Turtle-
  Grafik gehört, wenn überhaupt, in die `pcl`-Bibliothek und nicht in
  den Diagramm-Editor
- **Arranger** (mehrere Diagramme auf einer Fläche). Natter löst das
  über je eine `.pdiag` und den Projekt-Explorer


## Danach (Abschnitt 13.4: „Später“)

Jeweils eigener Schritt, baut auf der in Schritt 2–8 gebauten
Formen+Verbindungen-Infrastruktur auf (neue Formen-/Verbindungstypen,
kein neues Canvas-Grundgerüst nötig).

**Abgleich mit Dia** (September 2026, aus der lokalen Installation
`sheets/UML.sheet` ausgelesen): Dia bietet im UML-Blatt dreißig
Elemente. Natter hat davon bereits Klasse, Vorlageklasse, Anmerkung,
Paket (klein/groß), Abhängigkeit, Realisierung, Verallgemeinerung,
Assoziation und Aggregation. Es fehlen noch:

- Akteur, Anwendungsfall, Einschränkung → gehören zum
  **Use-Case-Diagramm** unten
- Lebenslinie, Objekt, Nachricht → **Sequenzdiagramm**
- Zustand, Anfangs-/Endzustand, Übergang → **Zustandsdiagramm**
- Aktivität, Verzweigung, Abzweigung/Vereinigung → **Aktivitätsdiagramm**
- Komponente, Facette, Buchse, Ereignisquelle, Ereignissenke, Knoten,
  Klassenstereotypsymbol → **Komponenten- und Verteilungsdiagramm**,
  das im Konzept gar nicht vorkommt. Für die Zielgruppe (Sekundarstufe)
  ist das entbehrlich; hier nur festgehalten, damit die Lücke bewusst
  ist und nicht übersehen wirkt

- [x] **Use-Case-Diagramm** – erledigt: Akteur (Strichmännchen mit dem
  Namen darunter), Anwendungsfall (Ellipse), Systemgrenze (ungefüllter
  Rahmen mit Namen oben), Notiz; Assoziation, «include», «extend»,
  Generalisierung. Zeichenfläche, Auswahl, Anordnen, Knickpunkte und
  Export kommen unverändert vom Klassendiagramm – ein neuer
  Diagrammtyp ist im Wesentlichen ein Eintrag in `FORMEN_JE_TYP` plus
  seine Darstellung
  - Der Akteur behält eine **feste** Höhe: würde das Männchen mit der
    Form mitwachsen, wäre ein breit gezogener Akteur ein grotesk
    breites Strichmännchen. In UML hat es immer dieselbe Gestalt, nur
    der Name darunter wächst
  - Notiz und Assoziation sind **dasselbe Objekt** wie im
    Klassendiagramm, keine zweite gleich aussehende Kopie:
    `form_art()` sucht über `kind`, und zwei Einträge mit derselben
    Kennung würden sich gegenseitig überschreiben, ohne dass es
    auffällt
  - Tests (23 neu): `tests/test_diagramm_use_case.py`
  - **Drei Fehler in der Sichtprüfung gefunden:** (1) der Layout-Prüfer
    meldete die Systemgrenze als Überlappung mit jedem Fall darin – das
    Diagramm hatte von Anfang an so viele Warnungen wie Fälle; ein
    Behälter, der eine Form ganz umschließt, zählt jetzt nicht mehr,
    einer der sie nur anschneidet weiterhin schon. (2) Das «extend»
    verschwand unter einer Ellipse, weil der Stereotyp vor den Formen
    gemalt wurde – derselbe Fehler wie seinerzeit bei den
    Multiplizitäten. (3) Danach zerschnitt er deren Namen; er liegt
    jetzt **neben** der Linie und teilt sich `label_offsets` mit den
    Multiplizitäten, lässt sich also mit der Maus wegziehen
- [x] **Aktivitätsdiagramm** – erledigt: Startknoten, Aktion
  (abgerundet), Entscheidung, Gabelung/Vereinigung (Balken),
  Objektknoten (**eckig** – genau daran unterscheidet man ihn von einer
  Aktion), Verantwortungsbereich (Swimlane), Endknoten, Ablaufende
  (Kreis mit Kreuz), Notiz; Kontrollfluss und Objektfluss. Die Bedingung
  eines Kontrollflusses steht in der Beschriftung der Mitte
  - Gabelung und Vereinigung sind **ein** Paletteneintrag: in UML
    dasselbe Zeichen; ob es teilt oder zusammenführt, sagen erst die
    Pfeile daran
  - Start, Ende und Entscheidung sind dieselben Objekte wie im
    Zustandsdiagramm
- [x] **Zustandsdiagramm** – erledigt: Startzustand (ausgefüllter
  Kreis), Zustand (abgerundetes Rechteck), zusammengesetzter Zustand,
  Entscheidung (Raute), Endzustand (Ring mit Kern), Notiz; Übergang mit
  `Ereignis [Bedingung] / Aktion`
  - **entry/do/exit ohne zweites Eingabefeld:** die **erste** Zeile des
    Namens ist der Name, jede weitere eine Aktion. Damit bleibt die
    Bedienung dieselbe wie bei einer Notiz – Doppelklick, tippen,
    fertig –, die Datei bleibt lesbar, und es braucht keinen eigenen
    Dialog für drei Textzeilen
  - Neu für **alle** Verbindungsarten: eine Beschriftung in der
    **Mitte** der Linie. Im Zustandsdiagramm trägt sie den ganzen
    Übergang, im Klassendiagramm kann sie den Namen einer Assoziation
    tragen. Sie liegt neben der Linie, ist hinterlegt und lässt sich
    wie die Multiplizitäten mit der Maus wegziehen
  - Tests (22 neu): `tests/test_diagramm_zustand.py`
  - **Zwei Fehler in der Sichtprüfung gefunden:** die Mindestbreite maß
    Name und Aktionen als **eine** Zeile und verlangte 627 px für einen
    Kasten, in den jede Zeile einzeln bequem passte; und die Meldung
    „ist zu schmal“ druckte den ganzen mehrzeiligen Namen und war in
    der Liste unlesbar
- [x] **Sequenzdiagramm** – erledigt: Lebenslinie (Kopf mit
  unterstrichenem Namen, darunter die gestrichelte Linie), Akteur mit
  Lebenslinie, Aktivierungsbalken, Fragment (`alt`/`opt`/`loop` im
  Reiter, Bedingung daneben), Zerstörung ✕, Notiz; synchrone Nachricht
  (gefüllte Spitze), asynchrone (offene), Antwort (gestrichelt) und
  «create»
  - **Der einzige Typ mit eigener Linienführung:** eine Nachricht läuft
    **waagerecht** auf einer festen Höhe, und diese Höhe *ist* die
    Reihenfolge. Von Mitte zu Mitte zu zeigen wäre hier sinnlos – bei
    nebeneinanderstehenden Lebenslinien lägen alle Nachrichten
    übereinander auf halber Höhe. Verschoben wird sie mit der Maus, als
    ein Undo-Schritt
- [x] Tests (31 neu): `tests/test_diagramm_aktivitaet_sequenz.py`
- **Zwei Fehler in der Sichtprüfung gefunden:** der Akteur stand im
  Sequenzdiagramm als Strichmännchen **ohne** Lebenslinie da, und die
  Nachrichten an ihn endeten im Leeren – bleibt unter dem Namen Platz
  übrig, zieht er jetzt eine Linie. Und der Akteur des
  Sequenzdiagramms (80×400) überschrieb stillschweigend den des
  Use-Case-Diagramms (80×104), weil beide dieselbe Kennung trugen;
  dort stand plötzlich ein 400 Pixel hoher Akteur. Die Kennungen sind
  jetzt getrennt, **und** der Aufbau der Nachschlagetabelle wirft bei
  zwei verschiedenen Einträgen mit derselben Kennung einen Fehler –
  diese Fehlerklasse kann nicht mehr stillschweigend auftreten
- [x] Abnahme: für jeden der vier Typen ein Beispiel von Hand
  nachgebaut und angesehen – Bibliotheksausleihe (Use-Case), Ampel
  (Zustand), Bestellung mit zwei Verantwortungsbereichen (Aktivität),
  Buchausleihe über vier Lebenslinien (Sequenz)

## Zurückgestellt (aus dem Konzept, „Später möglich“)

- [ ] ER-Diagramm (modifizierte Chen-Notation)
- [ ] Syntaxdiagramm
- [ ] Import von DIA-Dateien (`.dia`)
